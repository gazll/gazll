{
  "sections": [
    {
      "title": "IoC container & transactions",
      "items": [
        {
          "id": "03-spring-boot-deep-build.ioc-container-transactions.q1",
          "q": "How does the IoC/DI container work? Bean scopes and lifecycle?",
          "a": "The container [[g:inverts control]]: you declare beans, the container creates/wires/manages their lifecycle.\n\nDI removes manual `new` calls → easier to test (mock), easier to swap out.\n\n**Scope:**\n- [[b:singleton]] (default): 1 instance/context\n- [[b:prototype]]: a new instance every time it's fetched\n- web scope: [[b:request/session]]\n\n**Lifecycle:** instantiate → DI → `@PostConstruct` → use → `@PreDestroy`.\n\n:::warn Trap\ninjecting prototype into a singleton → [[r:it's only created once]]. You need `ObjectProvider`/`@Lookup` to get a fresh instance each time.\n:::\n\n:::deep\nThe container has two phases:\n- [[b:BeanDefinition]]: reads metadata (can be modified via `BeanFactoryPostProcessor`)\n- [[b:instantiation]]: DI, `BeanPostProcessor` wraps the AOP proxy\n\nPrefer [[g:constructor injection]] (immutable, easy to test, detects circular dependencies at startup) over field injection (hides dependencies, hard to test, forces reflection).\n\n- a circular dependency between two constructor-injected singletons → [[g:fail-fast]] (good); fix by splitting responsibilities, [[r:don't use `@Lazy` to hide a design smell]]\n- request/session scope needs a scoped proxy to be injected into a singleton\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.ioc-container-transactions.q2",
          "q": "How does @Transactional actually run? Why does self-invocation lose the transaction?",
          "a": "Spring uses an [[g:AOP proxy]] (JDK dynamic proxy if there's an interface, CGLIB if it's a class). The proxy opens/commits/rolls back the tx around the method.\n\nThe logic lives in the outer proxy, [[o:not in the bean's own bytecode]].\n\n[[r:Self-invocation]]: calling `this.method()` within the same bean → goes straight to `this`, bypassing the proxy → the annotation has no effect.\n- fix: split into a separate bean, self-injection, or AspectJ weaving\n- similarly: `private`/`final` methods can't be proxied\n\n**Code where the transaction is silently lost:**\n<pre><code><span class=\"k\">@Service</span>\n<span class=\"k\">class</span> BookingService {\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">confirmAll</span>(List&lt;Long&gt; ids) {\n        <span class=\"k\">for</span> (Long id : ids) {\n            <span class=\"f\">confirm</span>(id);      <span class=\"r\">// = this.confirm(id) → doesn't go through the proxy → NO transaction</span>\n        }\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">@Transactional</span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">confirm</span>(Long id) { <span class=\"c\">/* ... */</span> }\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// Fix: split into another bean, so the call leaves and re-enters through the proxy</span>\n<span class=\"k\">@Service</span>\n<span class=\"k\">class</span> BookingConfirmer {\n    <span class=\"k\">@Transactional</span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">confirm</span>(Long id) { <span class=\"c\">/* ... */</span> }\n}</code></pre>\n\n:::warn Money trap\nby default only RuntimeException/Error trigger rollback. A checked exception [[r:does NOT auto-rollback]] unless `rollbackFor` is set.\n:::\n\n:::deep\n- JDK dynamic proxy (with an interface) vs CGLIB (subclass — can't proxy `final`/`private`/`static`)\n- advice sits on the outer proxy → every self-invocation bypasses it\n- correct fixes: split into another bean, `AopContext.currentProxy()` (needs exposeProxy), or AspectJ weaving (also intercepts self-calls)\n\nThe transaction is bound to a [[b:thread]] via `TransactionSynchronizationManager` (ThreadLocal) → jumping to another thread (`@Async`, reactive, parallelStream) [[r:loses the tx context]].\n\n[[r:Critical fintech trap]]: calling an external API (PSP/GDS) inside a tx → holds the DB connection too long → pool exhaustion + prolonged locking.\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.ioc-container-transactions.q3",
          "q": "Propagation REQUIRED / REQUIRES_NEW / NESTED — when to use each?",
          "a": "<table class=\"cmp\"><thead><tr><th>Propagation</th><th>Behaviour</th><th>Use when</th></tr></thead><tbody>\n<tr><td>REQUIRED</td><td>joins the existing tx or creates a new one</td><td>default — shared commit/rollback</td></tr>\n<tr><td>REQUIRES_NEW</td><td>suspends the outer tx, opens an independent one</td><td>audit log must persist even if the business tx rolls back</td></tr>\n<tr><td>NESTED</td><td>savepoint within the current tx</td><td>partial rollback back to a savepoint</td></tr>\n</tbody></table>\n\n[[o:REQUIRES_NEW occupies 2 connections at once]] → watch the pool. Isolation can also be configured here (fintech usually uses READ COMMITTED/REPEATABLE READ + deliberate row locking).\n\n**Three nested methods and the resulting rollback:**\n<pre><code><span class=\"k\">@Transactional</span>                                <span class=\"c\">// TX-1 opens here</span>\n<span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">pay</span>(Long bookingId) {\n    ledger.<span class=\"f\">debit</span>(bookingId);                   <span class=\"c\">// REQUIRED  → runs within TX-1</span>\n    audit.<span class=\"f\">log</span>(<span class=\"s\">\"PAY_ATTEMPT\"</span>, bookingId);        <span class=\"c\">// REQUIRES_NEW → TX-2, commits separately</span>\n    promo.<span class=\"f\">apply</span>(bookingId);                   <span class=\"c\">// NESTED → savepoint within TX-1</span>\n    <span class=\"k\">throw</span> <span class=\"k\">new</span> PaymentFailedException();        <span class=\"c\">// TX-1 rolls back</span>\n}\n<span class=\"c\">// Result: the debit is rolled back, promo is lost too (its savepoint sits inside TX-1),</span>\n<span class=\"c\">// but the audit row STILL remains — that's exactly why REQUIRES_NEW exists.</span>\n<span class=\"c\">    </span>\n<span class=\"k\">@Transactional</span>(propagation = Propagation.REQUIRES_NEW)\n<span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">log</span>(String type, Long id) { <span class=\"c\">/* ... */</span> }\n<span class=\"c\">    </span>\n<span class=\"k\">@Transactional</span>(propagation = Propagation.NESTED)\n<span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">apply</span>(Long id) { <span class=\"c\">/* an error here only rolls back to the savepoint */</span> }</code></pre>\n\n:::deep\n- `REQUIRES_NEW` suspends the outer tx and opens a [[r:second connection]] → doubles the pressure on HikariCP under load, prone to pool deadlock (the outer tx holds connection A while waiting on connection B when the pool is exhausted)\n- `NESTED` uses a JDBC savepoint (not every DB/driver supports it well)\n\n**OTA deep dive:**\n- writing *audit/outbox* entries should [[g:share the tx (REQUIRED)]] to stay atomic with the business operation\n- REQUIRES_NEW only makes sense when the audit event must be saved even if the business operation fails\n:::",
          "difficulty": "hard"
        },
        {
          "id": "03-spring-boot-deep-build.ioc-container-transactions.q4",
          "q": "Spring AOP: how does the proxy work, and what are its limitations?",
          "a": "AOP separates [[g:cross-cutting concerns]] (tx, logging, security, retry) from business logic via *advice* wrapped around a *pointcut*.\n\nSpring AOP is [[b:proxy-based (runtime)]]:\n- it only intercepts calls made *through* the proxy to a Spring bean's public methods\n- it [[r:does not intercept]] self-calls, private methods, or objects created with `new`\n\nIf you need more power (constructors, fields, self-calls) → use [[b:AspectJ]] (compile-time/load-time weaving).\n\n**Why `private` and `final` methods can't be advised:**\n<pre><code><span class=\"k\">@Service</span>\n<span class=\"k\">class</span> PricingService {\n<span class=\"c\">    </span>\n    <span class=\"k\">@Cacheable</span>(<span class=\"s\">\"fares\"</span>)\n    <span class=\"k\">public</span> Fare <span class=\"f\">lookup</span>(String route) { <span class=\"c\">/* ... */</span> }      <span class=\"c\">// works: called from outside through the proxy</span>\n<span class=\"c\">    </span>\n    <span class=\"k\">@Cacheable</span>(<span class=\"s\">\"fares\"</span>)\n    <span class=\"k\">private</span> Fare <span class=\"f\">lookupRaw</span>(String route) { <span class=\"c\">/* ... */</span> }   <span class=\"r\">// silently does nothing</span>\n<span class=\"c\">    </span>\n    <span class=\"k\">@Cacheable</span>(<span class=\"s\">\"fares\"</span>)\n    <span class=\"k\">public</span> <span class=\"k\">final</span> Fare <span class=\"f\">lookupFinal</span>(String r) { <span class=\"c\">/* ... */</span> } <span class=\"r\">// silently does nothing</span>\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// CGLIB generates a subclass at runtime — whatever can't be overridden can't be advised:</span>\n<span class=\"c\">// class PricingService$$SpringCGLIB extends PricingService {</span>\n<span class=\"c\">//     public Fare lookup(String r) { ...advice...; return super.lookup(r); }  // OK</span>\n<span class=\"c\">//     private/final methods can't be reached by the subclass → advice can never be inserted</span>\n<span class=\"c\">// }</span></code></pre>\n\n:::deep\n- AspectJ inserts bytecode directly → can intercept constructors, field access, self-calls, non-Spring objects, but the build is more complex (weaver/agent)\n- ordering multiple aspects via `@Order` (important when tx + retry + logging overlap)\n- performance: the proxy adds one extra call layer (negligible); an overly broad pointcut makes reasoning harder\n\nDeep dive: typical cross-cutting concerns besides tx are retry, rate-limit, audit, metrics — [[r:but don't hide business logic inside an aspect]].\n:::",
          "difficulty": "hard"
        }
      ]
    },
    {
      "title": "Auto-configuration & build",
      "items": [
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q1",
          "q": "How does Spring Boot auto-configuration work under the hood?",
          "a": "Boot scans auto-config classes (declared in `...AutoConfiguration.imports`), applying them conditionally via [[g:@Conditional]]:\n- `@ConditionalOnClass` — is the library on the classpath?\n- `@ConditionalOnMissingBean` — has the user not already defined one?\n- `@ConditionalOnProperty` — enabled by a property?\n\n→ 'HikariCP is on the classpath ⇒ auto-configure a DataSource'; if you declare your own bean, Boot [[g:steps aside]].\n\n:::tip Bottom line\n'convention over configuration' = auto-config + starters. Debug with `--debug` to see the conditions report (matched/unmatched).\n:::\n\n:::deep\nBoot reads the auto-config list from `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` (replacing the old `spring.factories`), orders them with `@AutoConfigureBefore/After/Order`, and applies `@Conditional` in this order: classpath → user bean → property → web type.\n\n- writing a company-wide [[g:custom starter]] = an auto-config module + `OnMissingBean` (so other teams can override) + `@ConfigurationProperties`\n- [[r:Trap]]: wrong auto-config ordering causes wrong bean overrides; too many conditionals slow down startup\n:::",
          "difficulty": "hard"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q2",
          "q": "What is a starter dependency and why is it convenient?",
          "a": "A starter (e.g. `spring-boot-starter-web`) is a POM 'bundle' that gathers compatible dependencies for a single purpose (web = Spring MVC + Tomcat + Jackson...).\n\nOne line instead of declaring and aligning versions for dozens of libraries. Versions are aligned by Boot's [[g:parent BOM]].\n\n:::warn Interview trap\nThe follow-up question is almost always 'how does a starter differ from auto-configuration?'. A starter is just a POM that pulls in dependencies, [[o:it contains no configuration logic]]; the actual auto-configuration lives in `spring-boot-autoconfigure` with `@Conditional` classes (03-spring-boot-deep-build.auto-configuration-build.q1). Proof: pull in a starter and exclude auto-config, it still runs; conversely you can declare the dependency yourself and auto-config still kicks in.\n:::\n\n:::deep\n- a starter is a 'meta' POM (little/no code) that bundles compatible dependencies + activates the matching auto-config\n- transitive versions are aligned by the `spring-boot-dependencies` BOM → declaring a dependency [[g:needs no version]]\n- upgrading Boot means upgrading the whole 'basket' of libraries that have been tested together (reduces jar hell)\n- writing your own `company-payments-starter` standardizes config/observability across services (platform engineering)\n\n[[r:Trap]]: overriding a transitive version in the BOM must be done carefully (can break tested compatibility); use `dependency:tree` to check.\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q3",
          "q": "Deep-dive POM: dependencyManagement, BOM, scope, transitive dependencies, exclusion — what do you need to know?",
          "a": "- [[b:dependencyManagement / BOM]]: centralizes version declarations (import BOM with scope `import`); child modules inherit them\n- [[b:scope]]: `compile` (default), `provided` (supplied by the container), `runtime` (JDBC driver), `test`, `import`\n- [[b:transitive dependency]]: a dependency of a dependency; conflicts are resolved by [[o:'nearest wins']]\n- [[b:exclusion]]: excludes an unwanted transitive dependency\n- distinguish *plugin* (build-time) from *dependency* (the app's libraries)\n\nGolden command: `mvn dependency:tree` to inspect and untangle conflicts.\n\n:::tip Bottom line\ntransitive + nearest-wins + BOM are the key to avoiding 'jar hell' and `NoSuchMethodError` caused by version mismatches.\n:::\n\n:::deep\n- [[o:nearest-wins]] resolves conflicts (shallowest depth wins, NOT the highest version) → can sometimes pull in an unexpectedly old version, so pin it explicitly in `dependencyManagement` to [[g:'pin']] it\n- `&lt;scope&gt;import&lt;/scope&gt;` in dependencyManagement imports a BOM (e.g. Spring Cloud)\n- `optional=true` stops a transitive dependency from propagating further; `provided` for things supplied by the container (servlet/lombok)\n- `dependency:tree -Dverbose` + `dependency:analyze` (used-but-undeclared / declared-but-unused)\n\nDeep dive: runtime `NoSuchMethodError`/`NoClassDefFoundError` is usually caused by mismatched transitive versions → pinning the BOM + exclusions fixes 'jar hell' at the root.\n:::",
          "difficulty": "hard"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q4",
          "q": "Maven vs Gradle — what's the difference, and when to pick which?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>Maven</th><th>Gradle</th></tr></thead><tbody>\n<tr><td>Syntax</td><td>declarative XML</td><td>DSL (Groovy/Kotlin)</td></tr>\n<tr><td>Speed</td><td>stable, slower</td><td>incremental + build cache + daemon → fast</td></tr>\n<tr><td>Customization</td><td>strict convention</td><td>flexible build logic</td></tr>\n<tr><td>Good for</td><td>simple projects, easy onboarding</td><td>large / multi-module projects</td></tr>\n</tbody></table>\n\n:::deep\n- Gradle is fast thanks to [[g:incremental build]] (only rebuilds what changed) + [[g:build cache]] (reuses output, even remote/CI) + configuration cache + daemon → CI time drops from minutes to seconds for small changes\n- Maven compensates with [[g:determinism]], strict convention, easy to read/onboard, a huge plugin ecosystem, and 'no custom code' means fewer build bugs\n\nDeep dive: pick Gradle for monorepos/multi-module projects needing speed &amp; complex build logic (Android/Kotlin); pick Maven for simplicity and stability. Both support reproducible builds &amp; BOMs (supply-chain security).\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q5",
          "q": "Spring profiles & externalized config — best practices for managing configuration?",
          "a": "Separate config from code ([[g:12-factor]]): `application-{profile}.yml`, env vars, secret managers.\n\n**Priority order (highest → lowest):**\n- command-line args\n- env vars\n- `application-{profile}.yml`\n- `application.yml`\n- defaults in code\n\n[[r:Never hardcode secrets]] in the repo; use Vault/Secrets Manager + a config server for multiple services.\n\n:::deep\n- do NOT commit secrets to the repo/image; inject them at runtime via env/volume from Vault/AWS Secrets Manager/K8s Secret (+KMS)\n- profiles for environment (`dev/staging/prod`) and for features (`@Profile`)\n- for multiple services → Spring Cloud Config/Consul + dynamic refresh (`@RefreshScope`) to change config without redeploying\n\n- [[r:Trap]]: a profile-specific override silently causes 'works fine locally, breaks in prod'; storing sensitive config in a ConfigMap instead of a Secret\n- config is a kind of API too — version it and audit changes (GitOps)\n:::",
          "difficulty": "ext"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q6",
          "q": "@Async and @Scheduled — what should you watch out for?",
          "a": "- [[b:@Async]] runs a method on a different thread pool (also proxy-based → self-calls have no effect); configure a dedicated executor, [[r:don't rely on the default unbounded pool]]\n- [[b:@Scheduled]] for cron/fixed-rate jobs; with multiple instances you must avoid duplicate runs (ShedLock/distributed lock or leader election)\n\n:::deep\n[[r:Production trap]]: `@Async` defaults to `SimpleAsyncTaskExecutor` (spawns a new thread every time, NOT bounded) → under high load this explodes thread count/OOMs.\n- you must define a dedicated `ThreadPoolTaskExecutor` (core/max/queueCapacity)\n- remember `queueCapacity` must fill up before the pool grows threads toward max (a surprising behaviour)\n\n- `@Async` is also proxy-based → self-calls have no effect, and it loses tx/security/MDC context (needs a TaskDecorator to propagate MDC/trace id)\n- `@Scheduled` across N instances runs N times → use ShedLock/distributed lock or leader election\n\nDeep dive: Java 21 — a virtual-thread executor (`newVirtualThreadPerTaskExecutor`) for `@Async` scales I/O-bound work well without pool tuning.\n:::",
          "difficulty": "ext"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q7",
          "q": "Spring Boot Actuator & observing an application?",
          "a": "Actuator exposes operational endpoints:\n- `/health` (liveness/readiness groups)\n- `/metrics` (Micrometer → Prometheus)\n- `/info`, `/loggers`...\n\nIntegrates with Micrometer + OpenTelemetry to export metrics/traces. [[r:Remember to protect sensitive endpoints]] (auth) + only expose what's needed.\n\n:::deep\n- Actuator + Micrometer is a [[g:facade]] that exports metrics to Prometheus/Datadog/OTel (swap the backend without changing code)\n- the `/actuator/health` liveness vs readiness groups map directly to K8s probes: readiness includes dependencies (DB, broker), liveness only means 'the process is alive' (avoids a restart loop when the DB is slow)\n- custom `HealthIndicator` for business dependencies (GDS/PSP); protect `/env`, `/heapdump` with auth\n\n- wire up Micrometer Tracing + OTel → trace_id flows across services; export custom business metrics (`booking_failed_total`) to alert on SLOs\n- [[r:Operational trap]]: high tag cardinality (userId, bookingId) 'blows up' the Prometheus time-series → only tag bounded dimensions (status, endpoint). Use *exemplars* to jump from a metric to its trace.\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q8",
          "q": "Validation (Bean Validation) and centralized error handling?",
          "a": "Use `@Valid` + annotations (`@NotNull`, `@Size`, custom constraints) to validate at the [[b:boundary]] (controller/DTO).\n\nCatch errors centrally with `@ControllerAdvice` + `@ExceptionHandler` → return a standard error structure (RFC 7807 problem+json).\n\nSeparate [[g:syntactic validation]] (at the boundary) from [[g:business rules]] (in the domain).\n\n:::deep\n- [[b:syntactic/format]] at the boundary: DTO + `@Valid` + JSR-380 (`@NotNull`, `@Email`, `@Positive`), validation groups for create vs update\n- [[b:business rules]] in the domain: e.g. 'seat count ≤ passenger count' belongs to the aggregate, not an annotation\n- custom constraints for reusable rules (valid airport code)\n- centralized error handling with `@RestControllerAdvice` → problem+json with field errors\n\n- [[r:Trap]]: validating on the client and then trusting it — you must re-validate on the server (never trust input)\n- cross-field validation (return date &gt; departure date) uses a class-level constraint; don't cram complex business rules into an annotation\n:::",
          "difficulty": "ext"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q9",
          "q": "How do you configure HikariCP in Spring Boot for a high-traffic service?",
          "a": "Treat HikariCP as a [[g:bounded admission-control boundary]], not a knob that makes the database faster. With `spring-boot-starter-jdbc` or `spring-boot-starter-data-jpa`, Spring Boot brings HikariCP onto the classpath and prefers it when auto-configuring a pooled `DataSource`. Configure the pool only after defining a fleet-wide connection budget and a deadline hierarchy.\n\n<pre><code>spring:\n  datasource:\n    hikari:\n      pool-name: booking-write\n      maximum-pool-size: 12       <span class=\"c\"># illustrative; derive from measured capacity + fleet budget</span>\n      connection-timeout: 1500    <span class=\"c\"># milliseconds waiting to borrow; not connect/query time</span>\n      validation-timeout: 500     <span class=\"c\"># must be &lt; connectionTimeout</span>\n      max-lifetime: 1740000       <span class=\"c\"># only if infrastructure max age is known to be 30 min</span>\n      keepalive-time: 120000      <span class=\"c\"># only when idle paths otherwise expire connections</span></code></pre>\n\nThe numbers above are a configuration-shape example, [[r:not defaults or a recommendation]]. In particular:\n\n- derive `maximumPoolSize` from the database's measured sustainable concurrency and the aggregate budget across peak replicas, rollout surge, jobs, migrations, and every `DataSource`;\n- if `minimumIdle` is left unset, Hikari defaults it to `maximumPoolSize`, effectively keeping a fixed-size pool; set it lower only when fewer idle sessions are worth slower refill during a burst;\n- `connectionTimeout` bounds only the wait for a pool slot. JDBC connect, socket/read, statement, transaction, lock, and HTTP-request deadlines are different controls;\n- `maxLifetime` retires a physical connection after it is returned; keep it below a *known maximum-age* limit. `keepaliveTime` pings only idle connections and must be lower than `maxLifetime`;\n- `leakDetectionThreshold` reports a connection checked out for too long. It is evidence of a possible leak or long transaction, not proof of either;\n- prefer JDBC4 `Connection.isValid()`; do not add `connectionTestQuery` unless the driver actually lacks JDBC4 validation.\n\n:::warn Transaction boundary\nNever perform remote HTTP, broker, filesystem, or user-waiting work after a transaction has acquired a connection. A 100 ms SQL operation inside a 5 s transaction occupies a pool slot for roughly 5 s, so the bottleneck is hold time rather than query time.\n:::\n\n:::deep\nWatch active, idle, pending, acquisition timeouts, acquisition duration, usage duration, and creation failures through Actuator's `jdbc.connections` and Hikari-specific metrics. A growing pending queue means demand is waiting; it does not tell you whether the cause is a small pool, a saturated database, a lock, a leaked connection, or a slow external call inside the transaction.\n\nGeneric sizing logic lives in (25-microservice.02-pool-exhaustion-connection-thread.q1), while exact Hikari parameter semantics are in (25-microservice.02-pool-exhaustion-connection-thread.q3). The Spring lifecycle and engine-specific resource effects are covered in (03-spring-boot-deep-build.auto-configuration-build.q12), (03-spring-boot-deep-build.auto-configuration-build.q13), and (03-spring-boot-deep-build.auto-configuration-build.q14).\n:::",
          "difficulty": "ext"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q10",
          "q": "Testing strategy for a Spring Boot service: test pyramid, slice tests, Testcontainers, contract tests?",
          "a": "Follow the [[g:test pyramid]]:\n- lots of [[b:unit tests]] (JUnit 5 + Mockito, pure domain logic, milliseconds)\n- fewer: [[b:integration tests]]\n- on top: a handful of [[b:e2e tests]]\n\n- Spring [[g:slice tests]] (`@WebMvcTest`, `@DataJpaTest`) load only part of the context → fast\n- test real infrastructure (DB, Kafka) with [[g:Testcontainers]] (Docker) instead of H2, to stay close to production\n- [[g:contract tests]] (Spring Cloud Contract/Pact) keep provider–consumer compatibility from breaking when APIs change\n\n:::deep\n[[r:Anti-pattern]]: cramming every test into `@SpringBootTest` (loads the whole context &#8594; slow, brittle) instead of using slice/unit tests.\n\n**[[r:H2 lies]]**: its dialect/locking/data types differ from MySQL/Postgres &#8594; tests pass green but production fails; Testcontainers runs the real engine inside Docker.\n\n- [[r:Flaky tests]] are usually caused by time/order/thread dependencies → replace `Thread.sleep` with Awaitility, isolate data per test\n- fintech/OTA: test *idempotency* (calling twice with the same key &#8594; one effect) + *concurrency* (two threads debiting money/booking a seat at once &#8594; correct final balance)\n- *property-based testing* (jqwik) for money calculations (BigDecimal, rounding); measure quality with *mutation testing* (PIT), not just coverage %\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q11",
          "difficulty": "ext",
          "q": "What platform baseline and migration plan should a Spring service use in the Boot 4 generation?",
          "a": "Treat a framework upgrade as a compatibility program, not a version-number edit. As reviewed on 2026-08-01, the current stable line is Spring Boot 4.1; its documented Java range is 17–26, while Spring Framework 7.0 is the stable framework generation. Pick one production JDK—often JDK 25 when the organization and vendors support it—and test that exact runtime. A library may need a lower `--release` target than the service that consumes it.\n\n<table class=\"cmp\"><thead><tr><th>Generation change</th><th>What to verify</th></tr></thead><tbody>\n<tr><td>Jakarta EE 11 level</td><td>Servlet 6.1, JPA 3.2, Bean Validation 3.1, container/provider compatibility, and any remaining `javax.*` dependencies</td></tr>\n<tr><td>Jackson 3 default</td><td>`tools.jackson` package/group changes, changed defaults, custom modules, persisted JSON, and public wire-contract snapshots; annotation packages remain a documented exception</td></tr>\n<tr><td>JSpecify nullness</td><td>compiler/static-analysis configuration, application annotations, generated code, overrides, generic types, and Kotlin interop</td></tr>\n<tr><td>Java 25 runtime</td><td>agents, bytecode tools, native libraries, container image, GC/heap flags, CDS/CRaC/AOT assumptions, and preview features kept behind an explicit policy</td></tr>\n</tbody></table>\n\n:::tip Migration sequence\n1. Record the current BOM, direct/transitive dependencies, Java runtime, removed/deprecated APIs, serialization fixtures, and operational baselines.\n2. Upgrade through a supported path; let Boot dependency management own versions unless a documented reason requires an override.\n3. Compile with all warnings visible, run unit/slice/contract/integration tests, and compare generated OpenAPI, JSON, database mappings, security rules, startup, memory, and latency.\n4. Canarize with rollback compatibility for database, messages, and stored JSON. Patch to the latest supported maintenance release rather than freezing on `.0`.\n:::\n\n:::deep\nJSpecify describes nullness; it does not insert runtime validation or make existing business data non-null. Jackson 2 and 3 can coexist temporarily for migration, but dual stacks increase ambiguity and dependency risk—give the bridge an owner and removal date. Do not assume a major Spring upgrade requires adopting every new feature; first preserve behavior, then introduce API versioning, resilience, AOT, or new observability capabilities as separately measurable changes.\n:::"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q12",
          "difficulty": "hard",
          "q": "How does Spring Boot wire HikariCP, and what happens from @Transactional to Connection.close()?",
          "a": "Spring Boot's `DataSourceAutoConfiguration` creates a pooled `DataSource` only when JDBC classes and a usable driver are present and the application has not supplied its own `DataSource`. Boot prefers HikariCP when it is available. Common credentials and URL bind from `spring.datasource.*`; implementation-specific properties bind from `spring.datasource.hikari.*`. Defining a custom `DataSource` makes the relevant auto-configuration back off, so multiple data sources also require explicit pool names, transaction managers, `@Primary` decisions, and metrics tags.\n\nThe imperative request path is approximately:\n\n<pre><code>HTTP request\n  -&gt; TransactionInterceptor\n  -&gt; JpaTransactionManager / DataSourceTransactionManager\n  -&gt; HikariDataSource.getConnection()\n  -&gt; Hikari proxy Connection\n  -&gt; JDBC driver + socket/TLS\n  -&gt; database session/backend\n<span class=\"c\">    </span>\nConnection.close()\n  -&gt; close tracked statements / roll back unfinished work / reset dirty state\n  -&gt; return the physical connection to Hikari's idle collection\n  -&gt; physically close only on eviction, failed validation, retirement, or pool shutdown</code></pre>\n\n`getConnection()` borrows an idle physical connection or creates one while the pool is below `maximumPoolSize`. When every connection is in use, the caller waits up to `connectionTimeout`. The object returned to application code is a proxy: calling `close()` releases it to the pool instead of closing the database socket. Spring's transaction manager or Hibernate binds the borrowed connection to the current imperative transaction, so repository calls inside that boundary share it until commit/rollback and cleanup.\n\nThis has three important consequences:\n\n- [[b:Transaction duration is connection hold time]] once a connection has been acquired. Query speed alone cannot explain pool pressure.\n- Hikari must reset observable state such as auto-commit, read-only, transaction isolation, catalog/schema, and network timeout before reuse. Application code must still close `ResultSet`/`Statement`/`Connection` deterministically; pool cleanup is a safety net, not an ownership model.\n- `spring.datasource.connection-fetch=lazy` in the Boot 4.1 generation can wrap the auto-configured pool so a transaction that never creates a JDBC `Statement` never borrows a physical connection. It does not make SQL non-blocking or increase database capacity.\n\n**What exists in application memory per physical connection?** Hikari keeps pool bookkeeping and proxy state; the driver owns protocol/session objects, prepared-statement metadata or caches when enabled, authentication/TLS state, and socket buffers/file descriptors. Active `ResultSet`, LOB buffers, ORM entities, Hibernate's first-level cache, and DTO materialization usually dominate request memory, but they are request/transaction state rather than an idle Hikari entry. A larger pool indirectly permits more of that active work to exist concurrently.\n\n:::warn Hidden connection retention\n`@Transactional` around a remote call, a streaming response, an unbounded cursor, or slow application computation can retain a connection long after SQL stops. Open EntityManager in View extends the persistence context and permits late SQL during rendering, which can hide N+1 queries and acquire connections outside the intended service boundary; prefer `spring.jpa.open-in-view=false` plus explicit fetch plans/DTOs.\n:::\n\n:::deep\nLeaving `minimumIdle` unset gives Hikari a fixed-size target because its default equals `maximumPoolSize`. That favors burst responsiveness but makes every pod try to maintain its full allotment, which matters during cold starts and rolling deployments. An elastic pool (`minimumIdle` lower than max) reduces idle sessions but can create a connection storm when traffic jumps; measure authentication/TLS/session-setup latency and rate-limit rollout surge.\n\nChoose startup behavior deliberately with `initializationFailTimeout`: a service that cannot do useful work without the database may fail fast, while a component designed to start degraded may let the pool retry in the background. On graceful Spring shutdown, the `HikariDataSource` bean closes and physical connections are released; `kill -9`, OOM-kill, or a partition cannot rely on that lifecycle, so database-side idle/session limits and TCP keepalive remain necessary.\n\nThe proxy and transaction mechanics above connect directly to (03-spring-boot-deep-build.ioc-container-transactions.q2). Pool size and virtual-thread count are independent: cheap waiting threads do not create extra database capacity.\n:::"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q13",
          "difficulty": "hard",
          "q": "Why does the same Hikari maximumPoolSize have different memory and connection effects on MySQL, PostgreSQL, and Oracle?",
          "a": "`maximumPoolSize = N` means up to N physical JDBC connections for that pool, but a \"connection\" is not the same server-side object in every engine. It also has no universal MB cost: idle-session baseline, active statement memory, driver caches, TLS/socket buffers, execution processes/threads, parallel workers, and server configuration all vary.\n\n<table class=\"cmp\"><thead><tr><th>Engine</th><th>Server execution/session model</th><th>Memory and limit implications</th><th>Inspect / scale deliberately</th></tr></thead><tbody>\n<tr><td>MySQL 8.4</td><td>The classic handler associates a connection with a server thread; optional thread-pool/proxy products can change scheduling.</td><td>Each connection needs thread/session state such as a stack, network buffers and statement-digest memory. Sort/join/read/result buffers grow for active statements rather than being one fixed pre-allocation. <code>max_connections</code> is a slot ceiling, not a throughput target.</td><td><code>Threads_connected</code>, <code>Threads_running</code>, Performance Schema and <code>sys.memory_by_thread_by_current_bytes</code>. Preserve an administrative path instead of consuming every slot.</td></tr>\n<tr><td>PostgreSQL 18</td><td>Each client connection has a separate backend OS process unless an external pooler/proxy multiplexes clients.</td><td>Every backend has private state, and PostgreSQL also sizes some shared resources from <code>max_connections</code>. <code>work_mem</code> is per sort/hash operation—not per connection—and several plan nodes, parallel workers and sessions can multiply it; <code>temp_buffers</code> is session-local and allocated on demand.</td><td><code>pg_stat_activity</code>, OS RSS/PSS and <code>pg_backend_memory_contexts</code> for the current backend. A pooler can reduce backend count, but transaction pooling restricts session state.</td></tr>\n<tr><td>Oracle 26ai</td><td>Dedicated mode gives a user process its own server process; shared server uses dispatchers and a smaller server-process set; DRCP pools server processes/sessions across middle tiers.</td><td>Dedicated sessions place session memory in PGA; shared server moves UGA into SGA shared/large pool; DRCP changes the count that owns server/session memory. Capacity is constrained by <code>PROCESSES</code>, <code>SESSIONS</code>, PGA/SGA targets and workload—not just a JDBC pool value.</td><td><code>V$SESSION</code>, <code>V$PROCESS_MEMORY</code>, <code>PROCESSES</code>/<code>SESSIONS</code> and dispatcher/DRCP views. For RAC FAN, planned draining or Application Continuity, evaluate Oracle UCP instead of assuming a generic pool provides those events.</td></tr>\n</tbody></table>\n\nUseful snapshots—run with appropriate privileges and correlate them with application pool metrics:\n\n<pre><code><span class=\"c\">-- MySQL: connected vs executing, then current allocation by server thread</span>\nSHOW GLOBAL STATUS WHERE Variable_name IN ('Threads_connected','Threads_running');\nSELECT user, current_allocated FROM sys.memory_by_thread_by_current_bytes\nORDER BY current_allocated DESC LIMIT 10;\n<span class=\"c\">-- PostgreSQL: session state; the memory-context view describes this backend</span>\nSELECT state, count(*) FROM pg_stat_activity\nWHERE backend_type = 'client backend' GROUP BY state;\nSELECT name, total_bytes FROM pg_backend_memory_contexts\nORDER BY total_bytes DESC LIMIT 10;\n<span class=\"c\">-- Oracle: session/server mode and aggregate process-memory categories</span>\nSELECT server, status, COUNT(*) FROM v$session\nWHERE type = 'USER' GROUP BY server, status;\nSELECT category, SUM(allocated) FROM v$process_memory GROUP BY category;</code></pre>\n\nOn the JVM side, every physical connection adds at least a Hikari entry, driver object graph, socket/file descriptor, and possibly per-connection prepared-statement metadata/cache. The expensive amplification is often *indirect*: N active connections permit N concurrent result sets, ORM persistence contexts, LOB reads, JSON mappings, and database work areas. Therefore JVM heap, native/kernel socket memory, and database memory must be measured separately under the same concurrency.\n\n:::warn Do not multiply a folklore constant\nStatements such as \"one PostgreSQL connection costs 10 MB\" or \"Oracle always costs more\" erase engine version, extensions, session settings, query shape, shared/dedicated mode and allocator behavior. Measure idle baseline, then the delta for representative active queries and parallelism; use PSS rather than summing process RSS when shared pages matter.\n:::\n\n:::deep\nConnection-local caches multiply silently. A prepared-statement cache of K entries on each of N connections can create roughly N×K client entries plus server statements/cursors, while Hibernate's first-level cache is per persistence context and follows transaction/request cardinality instead. Size each cache from hit rate and memory/cursor limits, not because a blog calls it \"best practice.\"\n\nDatabase-side multiplexing changes semantics. PostgreSQL transaction-pooling proxies, Oracle shared server/DRCP, and vendor proxies reduce backend/process churn, but session variables, temporary tables, prepared statements, advisory locks, package state, and failover behavior may no longer have connection affinity. Verify the application's state assumptions before inserting another pooling layer, and choose one client-side pool rather than accidentally wrapping a pool inside another pool.\n\nFinally, an idle connection is not free, but an active query usually has a much larger and more variable footprint. `max_connections`/`PROCESSES` answers \"can another session log in?\"; load testing answers \"how many active queries meet the SLO without CPU, I/O, lock or memory collapse?\" Those are separate limits.\n:::"
        },
        {
          "id": "03-spring-boot-deep-build.auto-configuration-build.q14",
          "difficulty": "hard",
          "q": "How should you budget connections and align Hikari, JDBC, and database timeouts for autoscaling, rolling deploys, and failover?",
          "a": "Start with two ceilings: the database's [[b:hard session slots]] after administrative, replication, HA, maintenance, and other-application reserves; and its [[b:measured safe active-query concurrency]] for this workload. The lower operational boundary wins. Then calculate the worst live fleet, not today's replica count:\n\n<pre><code>fleet connection ceiling\n  = Σ(max live instances during rollout/failover × every pool maximum)\n  + batch workers + schedulers + migration clients\n<span class=\"c\">    </span>\nexample only: (8 replicas + maxSurge 2) × (12 primary + 8 replica) + 4 jobs = 204\n<span class=\"c\">The arithmetic exposes risk; it does NOT prove that 204 is safe.</span></code></pre>\n\nCount read/write pools, tenant-specific pools, sidecars, canaries and old/new versions during a rollout. If the ceiling exceeds the assigned budget, reduce per-pod pools or rollout surge, cap replicas, consolidate tenant pools, or introduce a semantics-compatible server/proxy pool. Raising the database's hard connection setting without CPU, memory, I/O and lock evidence can convert a clean \"too many connections\" rejection into a slow cluster-wide collapse.\n\n**Timeouts are layered contracts, not synonyms:**\n\n<table class=\"cmp\"><thead><tr><th>Control</th><th>What it bounds</th><th>Failure meaning</th></tr></thead><tbody>\n<tr><td>Hikari <code>connectionTimeout</code></td><td>Waiting to borrow an existing/new pool connection</td><td>Local admission queue exceeded its budget; no SQL necessarily ran</td></tr>\n<tr><td>Driver connect/login timeout</td><td>DNS/TCP/TLS/authentication and physical session creation</td><td>Cannot establish a new physical connection</td></tr>\n<tr><td>Statement/database query or lock timeout</td><td>SQL execution, lock wait, or server cancellation policy</td><td>The operation exceeded its DB budget; transaction may require rollback</td></tr>\n<tr><td>Driver socket/read timeout</td><td>Network read waiting for database bytes</td><td>Transport is silent; the driver may close or invalidate the connection</td></tr>\n<tr><td>Spring transaction timeout</td><td>Transaction-manager lifetime/resource deadline; it may propagate to JDBC statements</td><td>Can mark/roll back the transaction when enforced; it does not interrupt arbitrary Java or remote calls</td></tr>\n<tr><td>HTTP/request deadline</td><td>End-to-end service budget, including response serialization</td><td>Must be outside the inner budgets and leave time to return a controlled error</td></tr>\n<tr><td><code>maxLifetime</code>/<code>keepaliveTime</code></td><td>Connection lifecycle/idle liveness, not request execution</td><td>Retirement or health maintenance; neither cancels a slow query</td></tr>\n</tbody></table>\n\nAllocate inner budgets so pool wait + database work + application work fit inside the transaction/request deadline; reserve time for cleanup and error propagation. Configure driver properties explicitly and verify their units: MySQL Connector/J uses milliseconds for `connectTimeout`/`socketTimeout`; pgJDBC uses seconds for the similarly named properties; Oracle documents `oracle.jdbc.ReadTimeout` in milliseconds and has separate Oracle Net connect/keepalive controls. Copying the same numeric map across drivers can therefore change a 2-second intent into 2 milliseconds or leave it effectively unbounded.\n\n**Diagnose before increasing the pool:**\n\n<table class=\"cmp\"><thead><tr><th>Observed together</th><th>Likely class of problem</th><th>Next evidence/action</th></tr></thead><tbody>\n<tr><td>active=max, pending/acquire timeouts rise, usage duration rises, DB CPU modest</td><td>Long transaction, remote I/O inside transaction, lock wait, streaming result, N+1, or leaked checkout</td><td>Trace connection usage; inspect thread dumps, transaction spans, lock waits and slow queries; use leak detection only as supporting evidence</td></tr>\n<tr><td>active=max and DB CPU/I/O/locks plus query p99 are saturated</td><td>Database is the bottleneck</td><td>Tune query/index/model, shed/load-limit work; a larger pool usually adds queueing and contention</td></tr>\n<tr><td>total remains below max while creation failures rise</td><td>DNS/TLS/auth, firewall, exhausted DB slots, listener/proxy or failover problem</td><td>Inspect the actual driver exception and DB listener/session counters; pool sizing is not the first fix</td></tr>\n<tr><td>many idle connections across pods; outage begins during scale/rollout</td><td><code>minimumIdle</code>, maxSurge, multiple pools or tenant cardinality multiplied the fleet</td><td>Recompute the worst-case ceiling; slow/rate-limit startup and preserve administrative reserve</td></tr>\n</tbody></table>\n\n:::warn Failover is a transaction event\nA dead connection cannot preserve an in-flight local transaction. MySQL's `autoReconnect` is not a safe transaction-recovery strategy; Hikari can replace a broken connection for later work, but the caller must handle the failed unit. Retry only at an idempotent business boundary and treat a lost response after commit as an unknown outcome to reconcile, not proof that rollback occurred.\n:::\n\n:::deep\nAlign `maxLifetime` a little below a documented proxy/database *maximum age*, with Hikari's per-connection attenuation preventing mass retirement. Use `keepaliveTime` or TCP keepalive when infrastructure removes idle connections; do not confuse an idle timeout with maximum age. A low `minimumIdle` reduces steady idle sessions but makes recovery and scale-out depend on connection-creation capacity, so test a database restart and a full rollout, not only steady load.\n\nFor Oracle RAC event-driven invalidation, service draining and FAN/Application Continuity, Oracle UCP may be the appropriate client pool; Spring Boot supports choosing a pool implementation. Pick one pool intentionally rather than stacking UCP under Hikari. For PostgreSQL/MySQL proxies or multi-host drivers, verify read/write routing, DNS refresh, session state and transaction outcome semantics under real failover.\n\nThe operational dashboard should correlate `jdbc.connections`/`hikaricp` active-idle-pending-acquire-usage-creation metrics with database sessions, active queries, CPU/I/O, locks, query p99, JVM heap/native memory, pod count and deployment surge. That correlation distinguishes an application queue from a database saturation event. See the incident-level aggregate-budget trap in (25-microservice.02-pool-exhaustion-connection-thread.q2) and query-side remediation in (18-query-optimization.rewriting-the-query-reshaping-the-model.q4).\n:::"
        }
      ]
    },
    {
      "title": "Bean lifecycle & request flow",
      "items": [
        {
          "id": "03-spring-boot-deep-build.bean-lifecycle-request-flow.q1",
          "q": "The full bean lifecycle: what are the 8 stages, and where do the intervention hooks sit?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 250' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10' text-anchor='middle'>\n<defs><marker id='bl1' markerWidth='8' markerHeight='8' refX='6' refY='3' orient='auto'><path d='M0 0 L6 3 L0 6 z' fill='#42536C'/></marker></defs>\n<rect x='20' y='40' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='92.5' y='63' fill='#085239' font-size='10' font-weight='600'>1 · Instantiation</text><text x='92.5' y='78' fill='#42536C' font-size='8.5'>constructor</text>\n<path d='M167 66 L183 66' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<rect x='185' y='40' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='257.5' y='63' fill='#085239' font-size='10' font-weight='600'>2 · Populate</text><text x='257.5' y='78' fill='#42536C' font-size='8.5'>@Autowired / @Value</text>\n<path d='M332 66 L348 66' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<rect x='350' y='40' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='422.5' y='63' fill='#085239' font-size='10' font-weight='600'>3 · Aware</text><text x='422.5' y='78' fill='#42536C' font-size='8.5'>BeanName/Factory/Ctx</text>\n<path d='M497 66 L513 66' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<rect x='515' y='40' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='587.5' y='63' fill='#3D4E8F' font-size='10' font-weight='600'>4 · BPP (before)</text><text x='587.5' y='78' fill='#42536C' font-size='8.5'>postProcessBefore...</text>\n<rect x='20' y='145' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='92.5' y='168' fill='#085239' font-size='10' font-weight='600'>5 · Initialization</text><text x='92.5' y='183' fill='#42536C' font-size='8.5'>@PostConstruct / init</text>\n<path d='M167 171 L183 171' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<rect x='185' y='145' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='257.5' y='168' fill='#3D4E8F' font-size='10' font-weight='600'>6 · BPP (after)</text><text x='257.5' y='183' fill='#42536C' font-size='8.5'>AOP proxy wraps here</text>\n<path d='M332 171 L348 171' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<rect x='350' y='145' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='422.5' y='168' fill='#085239' font-size='10' font-weight='600'>7 · Ready to use</text><text x='422.5' y='183' fill='#42536C' font-size='8.5'>singleton in the context</text>\n<path d='M497 171 L513 171' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<rect x='515' y='145' width='145' height='52' rx='7' fill='#F6E8E5' stroke='#AE4536'/><text x='587.5' y='168' fill='#7d2f24' font-size='10' font-weight='600'>8 · Destruction</text><text x='587.5' y='183' fill='#42536C' font-size='8.5'>@PreDestroy / destroy()</text>\n<path d='M587 94 L587 118 L92 118 L92 141' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#bl1)'/>\n<text x='340' y='220' fill='#3D4E8F' font-size='9'>BeanPostProcessor runs for EVERY bean · step 6 is where the AOP proxy (@Transactional) gets wrapped</text>\n<text x='340' y='238' fill='#AE4536' font-size='9'>Prototype beans: the container does NOT call the destroy callback — you must clean up yourself</text>\n</svg></figure>\n\nThe container doesn't just `new` an object — it runs [[b:8 stages]] for every bean. Knowing the exact order is how you answer 'when does the AOP proxy get created' and 'why has `@PostConstruct` already seen its dependencies'.\n\n1. [[b:Instantiation]] — calls the constructor (constructor injection happens right here)\n2. [[b:Populate properties]] — sets fields/setters: `@Autowired`, `@Value`\n3. [[b:Aware callbacks]] — `BeanNameAware` → `BeanFactoryAware` → `ApplicationContextAware`\n4. [[b:BeanPostProcessor.postProcessBeforeInitialization]] — runs for *every* bean\n5. [[b:Initialization]] — `@PostConstruct` → `afterPropertiesSet()` (InitializingBean) → `init-method`\n6. [[b:BeanPostProcessor.postProcessAfterInitialization]] — [[g:where the AOP proxy gets wrapped]]\n7. [[b:Ready to use]] — the singleton sits in the container, serving requests\n8. [[b:Destruction]] — `@PreDestroy` → `destroy()` (DisposableBean) → `destroy-method`\n\n:::warn Trap\n[[r:Prototype]] beans: the container creates them and lets go — it [[r:never calls the destroy callback]]. Resources (files, sockets, executors) in a prototype bean must be closed manually.\n:::\n\n:::deep\n- `@PostConstruct` runs in step 5, [[o:before the proxy is wrapped in step 6]] → `this` at that point is the [[r:raw bean]]; calling your own `@Transactional`/`@Async`/`@Cacheable` method from within `@PostConstruct` has no effect\n- if you need to run after the context is fully ready, use `ApplicationRunner`/`CommandLineRunner` or `@EventListener(ApplicationReadyEvent.class)`\n- `BeanFactoryPostProcessor` modifies the [[b:BeanDefinition]] (metadata — before instantiation); `BeanPostProcessor` modifies the [[b:instance]] (after instantiation). Confusing the two is a classic mistake\n- BPPs are initialized very early → [[r:don't inject business beans into a BeanPostProcessor]]: that bean gets created before the infrastructure is ready and [[r:permanently loses its chance to be post-processed]] (no proxy → loses `@Transactional`)\n- `@PreDestroy` only runs on a graceful context shutdown (SIGTERM + shutdown hook); `kill -9`/an OOM-kill skips it → [[o:don't put critical business logic in a destroy callback]]\n- destruction order is the reverse of initialization order; to control start/stop order use `@DependsOn` or `SmartLifecycle` (with `getPhase()`)\n\n**Where each hook sits among the 8 stages:**\n<pre><code><span class=\"k\">@Component</span>\n<span class=\"k\">class</span> GdsClient <span class=\"k\">implements</span> InitializingBean, DisposableBean {\n<span class=\"c\">    </span>\n    <span class=\"f\">GdsClient</span>(HttpPool pool) { <span class=\"c\">/* ... */</span> }         <span class=\"c\">// 1 · constructor injection</span>\n<span class=\"c\">    </span>\n    <span class=\"k\">@Autowired</span> <span class=\"k\">void</span> <span class=\"f\">setClock</span>(Clock c) { }        <span class=\"c\">// 2 · populate field/setter</span>\n<span class=\"c\">    </span>\n    <span class=\"k\">@PostConstruct</span> <span class=\"k\">void</span> <span class=\"f\">warmUp</span>() {              <span class=\"c\">// 5 · after *Aware and BPP.before</span>\n        <span class=\"k\">this</span>.<span class=\"f\">refreshCache</span>();                     <span class=\"r\">// @Cacheable here has NO EFFECT: proxy not wrapped yet</span>\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">afterPropertiesSet</span>() { }          <span class=\"c\">// 5 · right after @PostConstruct</span>\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">destroy</span>() { }                     <span class=\"c\">// 8 · only runs on a graceful context shutdown</span>\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// The proxy (@Transactional, @Async, @Cacheable) is wrapped in stage 6 — AFTER stage 5.</span>\n<span class=\"c\">// If you need to run once everything is truly ready: @EventListener(ApplicationReadyEvent.class).</span></code></pre>\n:::",
          "difficulty": "core"
        },
        {
          "id": "03-spring-boot-deep-build.bean-lifecycle-request-flow.q2",
          "q": "How does a request flow through DispatcherServlet? Filter vs Interceptor vs ArgumentResolver vs MessageConverter?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 320' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10' text-anchor='middle'>\n<defs><marker id='ds1' markerWidth='8' markerHeight='8' refX='6' refY='3' orient='auto'><path d='M0 0 L6 3 L0 6 z' fill='#42536C'/></marker></defs>\n<rect x='20' y='34' width='145' height='52' rx='7' fill='#FFFFFF' stroke='#42536C'/><text x='92.5' y='57' fill='#14233A' font-size='9.5' font-weight='600'>Filter chain</text><text x='92.5' y='72' fill='#42536C' font-size='8.5'>Cors → Security → MDC</text>\n<path d='M167 60 L183 60' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='185' y='34' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='257.5' y='57' fill='#3D4E8F' font-size='9.5' font-weight='600'>DispatcherServlet</text><text x='257.5' y='72' fill='#42536C' font-size='8.5'>front controller</text>\n<path d='M332 60 L348 60' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='350' y='34' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='422.5' y='57' fill='#3D4E8F' font-size='9.5' font-weight='600'>1 HandlerMapping</text><text x='422.5' y='72' fill='#42536C' font-size='8.5'>matches @RequestMapping</text>\n<path d='M497 60 L513 60' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='515' y='34' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='587.5' y='57' fill='#3D4E8F' font-size='9.5' font-weight='600'>2 preHandle</text><text x='587.5' y='72' fill='#42536C' font-size='8.5'>Interceptor</text>\n<rect x='20' y='122' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='92.5' y='145' fill='#3D4E8F' font-size='9.5' font-weight='600'>3 HandlerAdapter</text><text x='92.5' y='160' fill='#42536C' font-size='8.5'>InvocableHandlerMethod</text>\n<path d='M167 148 L183 148' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='185' y='122' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='257.5' y='145' fill='#085239' font-size='9.5' font-weight='600'>3.1 ArgumentResolver</text><text x='257.5' y='160' fill='#42536C' font-size='8.5'>JSON → DTO, @Valid</text>\n<path d='M332 148 L348 148' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='350' y='122' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='422.5' y='145' fill='#085239' font-size='9.5' font-weight='600'>3.2 Controller</text><text x='422.5' y='160' fill='#42536C' font-size='8.5'>@RestController</text>\n<path d='M497 148 L513 148' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='515' y='122' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='587.5' y='145' fill='#085239' font-size='9.5' font-weight='600'>3.3 Service</text><text x='587.5' y='160' fill='#42536C' font-size='8.5'>@Transactional</text>\n<rect x='20' y='210' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='92.5' y='233' fill='#085239' font-size='9.5' font-weight='600'>3.4 Repository</text><text x='92.5' y='248' fill='#42536C' font-size='8.5'>Spring Data JPA</text>\n<path d='M167 236 L183 236' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='185' y='210' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='257.5' y='233' fill='#085239' font-size='9.5' font-weight='600'>3.5 ReturnValueHandler</text><text x='257.5' y='248' fill='#42536C' font-size='8.5'>picks a converter</text>\n<path d='M332 236 L348 236' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='350' y='210' width='145' height='52' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='422.5' y='233' fill='#085239' font-size='9.5' font-weight='600'>3.6 MessageConverter</text><text x='422.5' y='248' fill='#42536C' font-size='8.5'>Jackson serialize</text>\n<path d='M497 236 L513 236' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<rect x='515' y='210' width='145' height='52' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='587.5' y='233' fill='#3D4E8F' font-size='9.5' font-weight='600'>4 postHandle</text><text x='587.5' y='248' fill='#42536C' font-size='8.5'>6 afterCompletion</text>\n<path d='M587 86 L587 102 L92 102 L92 118' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<path d='M587 174 L587 190 L92 190 L92 206' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#ds1)'/>\n<text x='340' y='285' fill='#AE4536' font-size='9'>Exception anywhere → 5 HandlerExceptionResolver / @ControllerAdvice → JSON 4xx/5xx</text>\n<text x='340' y='303' fill='#3D4E8F' font-size='9'>6 afterCompletion ALWAYS runs → clears MDC/ThreadLocal before the thread returns to the pool</text>\n</svg></figure>\n\n`DispatcherServlet` is the [[g:front controller]]: it doesn't handle business logic, only dispatches.\n\n- [[b:1 HandlerMapping]] — finds the handler matching `@RequestMapping` (path, method, header, produces/consumes)\n- [[b:2 HandlerExecutionChain]] — runs `Interceptor.preHandle()` in registration order\n- [[b:3 HandlerAdapter]] — `InvocableHandlerMethod` invokes the controller method\n- [[b:3.1 ArgumentResolver]] — deserializes JSON → DTO, `@Valid`, `@RequestParam`, `@PathVariable`\n- [[b:3.2 → 3.4]] — Controller (`@RestController`) → Service (`@Transactional`) → Repository\n- [[b:3.5 ReturnValueHandler]] + [[b:3.6 HttpMessageConverter]] — Jackson serializes the object → JSON\n- [[b:4 postHandle]] — only runs when there is *no* exception\n- [[b:5 HandlerExceptionResolver]] — maps exceptions → HTTP status (`@ControllerAdvice`)\n- [[b:6 afterCompletion]] — always runs, even on error → cleans up MDC/ThreadLocal\n\n**Filter vs Interceptor:** a Filter belongs to the [[o:Servlet spec]], wrapping *outside* DispatcherServlet — it sees the raw request/response and can short-circuit (Security returns 401, rate limiting returns 429). An Interceptor belongs to [[o:Spring MVC]], sitting *inside* DispatcherServlet, so it knows which `HandlerMethod` is about to run.\n\n:::warn Trap\nAn exception thrown [[r:inside a filter]] (e.g. a broken token in the Security filter) [[r:does not go through `@ControllerAdvice`]] — it has already left DispatcherServlet. For a consistent JSON error body you must configure `AuthenticationEntryPoint`/`ErrorController`.\n:::\n\n:::deep\n- `@Transactional` commits at step 3.3, but Jackson serializes at [[b:3.6 — after the tx has already closed]] → touching a lazy collection at that point throws `LazyInitializationException`. Fix with a fetch join/DTO projection, [[r:not with `spring.jpa.open-in-view`]] (defaults to true: keeps the EntityManager open for the whole request → holds the connection longer and masks N+1 queries)\n- a filter that sets MDC/correlation id [[r:must call `MDC.clear()` in a `finally`]] — worker threads are reused, so without clearing, this request's log lines carry the previous request's traceId\n- filter order: `@Order`/`FilterRegistrationBean.setOrder`; Spring Security is actually *one* filter (`FilterChainProxy`) containing an internal chain of sub-filters\n- `produces`/`consumes` participate in handler selection and determine which `HttpMessageConverter` is used; duplicate mappings for the same path fail immediately at startup (`AmbiguousMappingException`)\n- `ArgumentResolver`/`ReturnValueHandler` are customized via `WebMvcConfigurer` — the standard place to implement `@CurrentUser` or a standardized response envelope\n:::",
          "difficulty": "hard"
        }
      ]
    }
  ]
}
