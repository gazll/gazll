{
  "sections": [
    {
      "title": "OAuth2 & OIDC",
      "items": [
        {
          "id": "13-security-oauth2.oauth2-oidc.q1",
          "q": "OAuth 2.x: các role, flow hiện đại và flow cần tránh là gì?",
          "a": "**Role:** [[b:resource owner]] cấp quyền; [[b:client]] yêu cầu quyền; [[b:authorization server]] authenticate/authorize rồi phát token; [[b:resource server]] enforce token cho API. OAuth là delegated [[g:authorization]], không phải protocol authenticate user; OpenID Connect bổ sung lớp đó.\n\nChọn flow theo loại client và user context:\n\n- **Authorization Code + PKCE** cho browser, native và client có user. BFF phía server có thể giữ token khỏi JavaScript; SPA thuần vẫn là public client.\n- **Client Credentials** khi confidential workload hành động dưới danh nghĩa chính nó, không phải user. Ưu tiên workload identity, private-key authentication hoặc federation thay cho shared secret sống lâu.\n- **Device Authorization Grant** cho thiết bị khó nhập liệu khi hệ thống hỗ trợ.\n- **Refresh Token Grant** gia hạn authorization đã có; việc phát refresh token là tuỳ policy và phải chống replay.\n- Tránh **Implicit** và **Resource Owner Password Credentials**. RFC 9700 khuyến nghị response bằng authorization code và không để client xử lý password user.\n\n:::deep\nOAuth 2.1 vẫn là [[o:Internet-Draft (draft-15 tại lần review năm 2026)]], chưa phải RFC. Dùng RFC 9700 — OAuth 2.0 Security BCP — làm hướng dẫn normative đã phát hành, đồng thời theo dõi draft để biết thay đổi hợp nhất.\n\nScope và audience phải least-privilege, gắn đúng resource. Client Credentials không được tự tạo end-user identity; khi service A gọi B thay user, thiết kế delegation/token exchange tường minh với audience được downscope thay vì forward mù token gốc.\n\nPasskey/WebAuthn và MFA là phương thức authenticate tại authorization server/identity provider, không phải OAuth grant. Chúng làm bước đăng nhập mạnh hơn, còn OAuth/OIDC quản lý token và delegation.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.oauth2-oidc.q2",
          "q": "Authorization Code + PKCE hoạt động ra sao và vì sao cần PKCE?",
          "a": "Client tạo `code_verifier` entropy cao, dẫn xuất S256 `code_challenge`, lưu verifier cùng transaction state rồi redirect browser tới authorization endpoint. Sau authenticate/consent, authorization server gửi code sống ngắn, dùng một lần về redirect URI đã đăng ký chính xác.\n\nClient đổi code ở token endpoint và chứng minh đang giữ verifier.\n\n<pre><code><span class=\"c\"># authorization request (xuống dòng chỉ để dễ đọc)</span>\nGET /authorize?response_type=code&amp;client_id=web&amp;\n    code_challenge=BASE64URL(SHA256(verifier))&amp;code_challenge_method=S256&amp;\n    state=BOUND_TO_BROWSER_SESSION&amp;redirect_uri=https%3A%2F%2Fapp.example%2Fcallback\n<span class=\"c\">    </span>\n<span class=\"c\"># token request back-channel: code bị lấy cắp vô dụng nếu thiếu verifier</span>\nPOST /token\ngrant_type=authorization_code&amp;code=ONE_TIME_CODE&amp;code_verifier=ORIGINAL_VERIFIER</code></pre>\n\nPKCE chống authorization-code interception và injection. RFC 9700 yêu cầu bảo vệ bằng PKCE cho public client; confidential client cũng nên dùng để defense in depth. OAuth 2.1 draft hiện đưa PKCE vào authorization-code flow mặc định, nhưng OAuth 2.1 [[r:chưa phải RFC]].\n\n:::deep\nPKCE chỉ là một control trong flow được bind thành transaction:\n\n- dùng S256, verifier entropy cao, code dùng một lần/sống ngắn và exact redirect-URI matching;\n- bind `state` với user-agent session để correlate request/response và chống CSRF; với OIDC còn validate `nonce` trong ID token và issuer của authorization response khi áp dụng;\n- khi validate token, kiểm issuer/audience để code hoặc token từ authorization server/resource khác không bị thay thế;\n- không đặt access token trong redirect URL.\n\nVới browser app, ưu tiên BFF nếu deployment hỗ trợ: BFF là confidential OAuth client, giữ token phía server và chỉ trả Secure/HttpOnly/SameSite session cookie có CSRF protection.\n\nSPA thuần dùng Authorization Code + PKCE, giảm token lifetime/storage, bật CSP mạnh và giả định XSS có thể hành động bằng mọi token JavaScript đọc được.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.oauth2-oidc.q3",
          "q": "OAuth2 vs OIDC khác gì?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>OAuth2</th><th>OIDC</th></tr></thead><tbody>\n<tr><td>Trả lời</td><td>'app được phép làm gì'</td><td>'user là ai'</td></tr>\n<tr><td>Token</td><td>Access Token</td><td>+ ID Token (JWT, `sub`, claims)</td></tr>\n<tr><td>Mục đích</td><td>authorization</td><td>authentication (+ authorization)</td></tr>\n</tbody></table>\n\nOIDC xây trên OAuth2, thêm ID Token + endpoint `/userinfo`. SSO hiện đại dùng OIDC.\n\n:::deep\n- OAuth2 trả *access token* (app được phép làm gì) — KHÔNG nói user là ai chuẩn hoá\n- OIDC thêm ID Token (JWT: `sub`, `iss`, `aud`, `email`, `auth_time`), `/userinfo`, discovery `/.well-known/openid-configuration`, JWKS\n\n[[r:Lỗi phổ biến]]: dùng *access token* để xác định danh tính user (sai — access token cho resource server); dùng ID token để gọi API (sai — ID token cho client biết user là ai).\n\nOTA: OIDC cho login B2C (qua Google/social IdP), access token gọi service nội bộ với scope phù hợp.\n:::",
          "difficulty": "core"
        }
      ]
    },
    {
      "title": "JWT & token",
      "items": [
        {
          "id": "13-security-oauth2.jwt-tokens.q1",
          "q": "Cấu trúc JWT, cách ký và xác thực?",
          "a": "Signed JWT thường dùng compact JWS `header.payload.signature`, mỗi phần base64url-encoded. Payload [[r:không được mã hoá]] trừ khi có thiết kế JWE riêng.\n\n- Header: `alg` đã pin, key ID `kid` để rotate và `typ` tường minh như access-token profile API mong đợi.\n- Claim: `iss`, `sub`, `aud`, `exp`, `iat`, tuỳ chọn `nbf`/`jti`, cùng authorization data như scope.\n- Signature: bảo đảm integrity/authenticity cho header và payload đã encode bằng trusted key.\n\n<pre><code><span class=\"c\">// Thứ tự validate; dùng JOSE/OAuth library được bảo trì, đừng tự viết crypto</span>\n<span class=\"f\">requireExpectedTypAndAllowedAlgorithm</span>(header);\nKey key = trustedJwks.<span class=\"f\">resolve</span>(header.kid());\nClaims c = jws.<span class=\"f\">verifyThenDecode</span>(key);\n<span class=\"f\">requireIssuer</span>(c, EXPECTED_ISSUER);\n<span class=\"f\">requireAudience</span>(c, THIS_API);\n<span class=\"f\">requireTimeWindow</span>(c.nbf(), c.exp(), ALLOWED_CLOCK_SKEW);\n<span class=\"f\">requireScopesAndSubjectPolicy</span>(c);</code></pre>\n\n:::warn Parse không phải validate\nKhông authorize từ claim chưa verify. Reject `alg: none`, confusion giữa algorithm/key type, `typ` ngoài dự kiến, `jku`/`x5u` không trusted, thiếu issuer/audience/time claim và token dành cho API hoặc token class khác. ID token không phải access token để gọi API.\n:::\n\n:::deep\nXử lý JWKS là logic production: chỉ fetch từ issuer configuration đã trust; cache key; rate-limit refresh khi gặp `kid` lạ; giữ key cũ hết maximum token lifetime; overlap signing key cũ/mới khi rotate. “Refetch với mọi unknown kid” ngây thơ tạo DoS vector, còn cache không refresh sẽ làm rotation hỏng.\n\n`typ`, issuer, audience, key set và validation rule cùng chống cross-JWT/token-substitution. Signature hợp lệ chỉ nói ai đã ký bytes; authorization vẫn phải kiểm tenant, subject, scope/role, resource ownership và policy hiện hành.\n\nGiữ claim tối thiểu, không đặt secret trong payload đọc được, không log token và chọn access-token lifetime theo replay risk cùng khả năng revoke thay vì một số phút chung cho mọi hệ thống.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.jwt-tokens.q2",
          "q": "JWT (stateless) vs opaque token / session — đánh đổi?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>JWT</th><th>Opaque + introspection</th><th>Session</th></tr></thead><tbody>\n<tr><td>Validate</td><td>cục bộ (không gọi AS)</td><td>hỏi AS mỗi lần</td><td>server-side state</td></tr>\n<tr><td>Revoke</td><td>khó (còn hạn vẫn valid)</td><td>tức thì</td><td>dễ</td></tr>\n<tr><td>Scale</td><td>tốt, latency thấp</td><td>thêm round-trip</td><td>cần sticky/shared store</td></tr>\n</tbody></table>\n\n:::tip Chốt\nJWT đổi khả năng revoke lấy scale. Fintech thường: access token JWT ngắn hạn + refresh token có thể revoke + blacklist/rotation.\n:::\n\n:::deep\n- cần revoke tức thì (khóa tài khoản gian lận) → access token ngắn hạn + introspection cho thao tác nhạy cảm, hoặc opaque token\n- [[g:mẫu lai phổ biến]]: access token JWT 5–15 phút (validate cục bộ) + refresh token *rotation* server-side (revoke được, phát hiện reuse → thu hồi cả họ) + denylist `jti` cho khẩn\n\n'JWT đổi khả năng revoke lấy tính phi trạng thái/scale; ta bù bằng TTL ngắn + revoke ở tầng refresh' — [[r:tránh nói 'JWT luôn tốt hơn session']].\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.jwt-tokens.q3",
          "q": "Access token vs refresh token và chiến lược thu hồi?",
          "a": "- [[b:Access token]] được gửi tới resource server và nên giới hạn audience, least-privilege, sống đủ ngắn theo replay/revocation risk.\n- [[b:Refresh token]] là credential giá trị cao hơn, chỉ dùng tại authorization server. Việc phát và lifetime là policy; inactivity, consent, credential, device hoặc risk change có thể kết thúc grant sớm.\n\nVới public client, RFC 9700 yêu cầu refresh token phải [[g:sender-constrained]] (ví dụ DPoP) hoặc rotate có reuse detection. Rotation phát token mới ở mỗi lần refresh, vô hiệu predecessor và revoke family đang active khi reuse cho thấy khả năng bị đánh cắp.\n\nConfidential client phải authenticate và server phải bind token với client đó.\n\nStorage theo threat model của platform: BFF giữ OAuth token phía server và dùng Secure/HttpOnly/SameSite cookie kèm CSRF defense; native app dùng OS-protected storage; browser-only app giảm lifetime và thường giữ access token trong memory.\n\nHttpOnly chặn JavaScript đọc token, không chặn XSS gửi request, nên CSP và output safety vẫn cần.\n\n:::deep\nRevocation có nhiều lớp:\n\n- revoke authorization/refresh-token family khi logout, rút consent, account compromise, reset credential hoặc reuse detection;\n- dùng opaque access token kèm introspection khi revoke tập trung tức thì đáng đổi lấy availability/latency cost, hoặc signed token sống ngắn khi local validation hợp hơn;\n- chỉ dùng denylist `jti` cho emergency case có bound, với retention/availability design tường minh;\n- yêu cầu reauthentication hoặc step-up bằng WebAuthn/MFA cho operation rủi ro cao dù token còn hợp lệ;\n- cân nhắc access token sender-constrained bằng DPoP hoặc mTLS khi bearer-token replay là rủi ro đáng kể.\n\nKhông log token hay URL/header chứa token. Redact lúc ingest, giới hạn quyền đọc security log và alert refresh reuse mà không ghi chính credential (20-observability-sre.slos-alerting-incidents.q4).\n:::",
          "difficulty": "core"
        }
      ]
    },
    {
      "title": "Thiết kế hệ auth",
      "items": [
        {
          "id": "13-security-oauth2.designing-the-auth-system.q1",
          "q": "Thiết kế hệ xác thực/uỷ quyền cho micro-services (SSO + gateway)?",
          "a": "Thiết kế riêng ba identity: human/session, OAuth client và workload đang gọi.\n\n- [[b:Authorization server/IdP]] trung tâm xử lý OIDC login, WebAuthn/passkey/MFA, consent, phát token, key và session risk.\n- [[b:Gateway hoặc BFF]] validate protocol ngoài ở edge, xoá mọi identity header client tự gửi rồi tạo internal context đáng tin. BFF giữ browser token phía server.\n- Mỗi [[b:service sở hữu resource]] enforce scope cùng domain authorization như tenant, object ownership, amount và state transition. Edge validation không thể quyết định các invariant đó.\n- Workload authenticate bằng federated identity sống ngắn, mTLS/SPIFFE hoặc OAuth confidential-client method; tránh một static credential dùng chung cho nhiều service.\n\n:::tip Chọn một internal token contract\nHoặc service validate token phát bên ngoài với đúng issuer/audience profile, hoặc trusted edge exchange nó thành internal token sống ngắn, đã downscope.\n:::\n\nĐừng forward identity header tuỳ ý hay mọi bearer token gốc qua tất cả hop. Khi service hành động thay user, giữ actor/delegation chain tường minh.\n:::deep\nValidation và authorization nằm ở các boundary khác nhau. Gateway có thể reject token malformed/expired, enforce coarse scope, rate-limit và translate protocol. Ledger vẫn phải kiểm subject này có quyền chuyển tiền từ account đó và yêu cầu step-up cho hành động rủi ro cao.\n\nVới local JWT validation, phân phối trust qua OIDC discovery/JWKS có cache bound và rotation overlap. Với opaque token, thiết kế availability, caching và revocation semantics của introspection.\n\nInternal call cần workload identity ngay cả khi có user context; một credential trả lời “service nào?”, delegated token trả lời “thay actor nào với quyền gì?”.\n\nPropagate absolute deadline, trace context và chỉ identity attribute tối thiểu. Log authorization decision cùng policy version nhưng không log credential hay sensitive claim.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q2",
          "q": "mTLS và xác thực service-to-service?",
          "a": "[[g:mTLS]] authenticate cả hai TLS peer bằng certificate đồng thời mã hoá channel. Nó chứng minh bên gọi giữ private key gắn với certificate/trust domain được chấp nhận; tự nó không quyết định workload có được thực hiện business action hay không.\n\nCác use case phổ biến:\n\n- workload-to-workload identity bằng certificate/SVID do mesh, workload-identity platform hoặc PKI automation rotate;\n- OAuth client authentication và certificate-bound access token theo RFC 8705, giảm replay từ TLS client khác;\n- B2B partner API khi hai tổ chức vận hành được issuance, renewal, revocation và incident procedure cho certificate.\n\nDPoP (RFC 9449) là proof-of-possession ở application layer, có thể sender-constrain token mà không cần cùng topology TLS client certificate. Cả hai cơ chế đều không cứu được client bị compromise mất cả token lẫn key.\n\n:::deep\nService mesh có thể automate issuance, rotation và transport policy, nhưng không phải “security không cần code hay vận hành”.\n\nPhải định nghĩa trust domain, authorization theo namespace/service, certificate lifetime, clock-skew behavior, CA/root rotation, xử lý revocation/compromise, telemetry và fail-open/fail-closed. Test để chắc plaintext bypass path đã đóng.\n\nDùng mTLS khi threat model và platform hợp; “mọi request bắt buộc qua mesh” không phải luật kiến trúc chung. Nó thêm proxy hoặc library integration, control-plane dependency, latency/resource cost và một policy layer nữa.\n\nCloud workload federation hoặc signed workload token có thể hợp một số boundary hơn.\n\nTách service identity khỏi end-user delegation. mTLS peer có thể là `booking-service`, còn scoped token nói nó hành động thay user U trên booking B. Authorization phải xét cả hai khi cả hai có ý nghĩa.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q3",
          "q": "OWASP Top 10:2025 và API Security Top 10:2023 nên định hình backend control thế nào?",
          "a": "Coi hai danh sách là [[o:input cho awareness và threat modelling]], không phải compliance checklist.\n\nOWASP Top 10:2025 nhấn mạnh broken access control, security misconfiguration, software supply-chain failure, lỗi cryptographic/injection/insecure-design/authentication, integrity, logging/alerting và xử lý exception sai.\n\nAPI Security Top 10:2023 bổ sung góc API như authorization theo object/function/property, resource consumption, sensitive business flow, SSRF, inventory và unsafe consumption của upstream API.\n\n<table class=\"cmp\"><thead><tr><th>Rủi ro</th><th>Engineering control</th></tr></thead><tbody>\n<tr><td>injection / XSS / CSRF</td><td>parameter binding; output encoding theo context và CSP; SameSite cùng CSRF token cho write dùng cookie auth</td></tr>\n<tr><td>BOLA/IDOR và authorization theo function/property</td><td>check tenant, object, field, action phía server ở mọi path; deny mặc định; test negative case</td></tr>\n<tr><td>lạm dụng resource/sensitive flow</td><td>payload/page limit, deadline, quota, bot/fraud control, idempotency và admission theo cost</td></tr>\n<tr><td>SSRF / upstream API không an toàn</td><td>destination allowlist, parse URL an toàn, egress control, validate response, timeout<br>và không forward redirect/credential mù</td></tr>\n<tr><td>supply chain và integrity</td><td>lock dependency, SCA, SBOM, provenance, artifact ký số, build cô lập và verify signature/policy lúc deploy</td></tr>\n</tbody></table>\n\n:::deep\nChống mass assignment bằng input DTO/allowlist tường minh, không phải giấu field trên UI. Error path cần cùng authorization/data-minimization rule như success path: fail closed khi correctness/security yêu cầu, trả problem detail ổn định và không lộ stack trace/secret.\n\nLập security test matrix từ asset và abuse case: cross-tenant ID, role downgrade, hidden field, payload lớn/sâu, race/replay, webhook SSRF, schema partner bất ngờ, dependency compromise, key rotation và policy/identity service unavailable.\n\nChạy SAST/SCA/secret scan sớm, DAST cùng authorization test trên hệ đã deploy, rồi verify audit/alert bằng incident exercise.\n\nWAF và rate limit là defense in depth, không thay resource-level authorization, safe code hay anti-automation cho business flow.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q4",
          "q": "Quản lý secrets đúng cách?",
          "a": "Ưu tiên [[g:không có secret sống lâu]] khi workload identity hoặc federation sống ngắn cung cấp được credential. Khi thật sự cần secret, giữ nó khỏi source, build log, image, CI variable có scope rộng và config phía client nhìn thấy.\n\n- lưu/phát qua managed secret/KMS/HSM với least-privilege identity và access có audit;\n- inject runtime qua channel platform có leakage risk đã hiểu (agent/file/volume/API; environment variable có thể xuất hiện trong dump hoặc diagnostic);\n- tách environment/tenant, giới hạn blast radius và dùng dynamic credential sống ngắn khi hỗ trợ;\n- rotate khi compromise, đổi personnel/ownership, hết cryptoperiod hoặc policy trigger; automate renewal và test overlap/rollback thay vì rotate mù gây outage;\n- scan code hiện tại, Git history, artifact, log, ticket và image; credential bị phát hiện phải revoke/reissue, không chỉ xoá khỏi commit mới nhất.\n\n:::deep\nKubernetes `Secret` là data base64-encoded, tự nó không phải encryption. Bật etcd encryption at rest, RBAC chặt, cô lập namespace/service account, audit và ưu tiên external secret/KMS integration cho material giá trị cao.\n\nRotate JWT signing key cần publish public key cũ/mới dưới `kid` riêng, chuyển signer, giữ verification key cũ hết maximum token lifetime cộng skew rồi mới retire. Rotation root/CA, database, webhook, PSP/GDS và encryption key cũng cần dependency order cùng rollback tường minh.\n\nDùng envelope encryption cho data lưu trữ: data-encryption key mã hoá data, còn key do KMS/HSM quản lý mã hoá DEK. Duy trì inventory owner, consumer, scope, thời điểm tạo/hết hạn, cách rotate và emergency revocation của secret.\n\nKhông log material; chỉ log secret identifier/version và access decision.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q5",
          "q": "Bảo vệ endpoint auth khỏi brute-force/credential stuffing?",
          "a": "- rate limit theo IP + theo tài khoản\n- exponential backoff/lockout tạm thời; CAPTCHA sau N lần fail\n- phát hiện bất thường (đăng nhập từ vị trí lạ)\n- lưu mật khẩu bằng hàm băm chậm có salt ([[g:bcrypt/argon2/scrypt]]), [[r:không MD5/SHA thường]]\n- MFA cho thao tác nhạy cảm\n- [[o:không tiết lộ 'user tồn tại hay không']] qua thông báo lỗi\n\n:::deep\n- rate limit + lockout theo IP *và* theo account (chống credential stuffing nhiều IP một account, &amp; brute-force một account một IP); exponential backoff; CAPTCHA sau ngưỡng; phát hiện bất thường (vị trí/thiết bị lạ → step-up MFA)\n- [[g:argon2id]] (khuyến nghị) / bcrypt / scrypt — KHÔNG MD5/SHA thường (nhanh, dễ brute-force GPU); không lộ 'user tồn tại' qua lỗi/timing (timing-safe)\n\nFintech/OTA: yêu cầu phishing-resistant hoặc step-up MFA khi transaction risk, quy định và recovery policy đòi hỏi; ưu tiên passkey/WebAuthn khi hỗ trợ, đồng thời xem device/location signal là risk input chứ không phải identity proof.\n\n[[r:Bẫy]]: lockout quá gắt → DoS chính user thật (kẻ tấn công khoá account nạn nhân) → CAPTCHA thay khoá cứng, hoặc khoá theo IP+account.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q6",
          "q": "RBAC vs ABAC — chọn mô hình phân quyền nào?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>RBAC</th><th>ABAC</th></tr></thead><tbody>\n<tr><td>Cơ sở</td><td>vai trò (admin, teller)</td><td>thuộc tính (user, resource, ngữ cảnh, thời gian)</td></tr>\n<tr><td>Ưu</td><td>đơn giản, dễ quản/audit</td><td>linh hoạt cho rule phức tạp</td></tr>\n<tr><td>Nhược</td><td>role explosion khi rule phức tạp</td><td>khó debug/audit</td></tr>\n</tbody></table>\n\nNhiều hệ kết hợp: RBAC nền + ABAC cho rule tinh.\n\n:::deep\n- ABAC ví dụ: 'agent chỉ duyệt booking &lt; hạn mức của chi nhánh mình trong giờ làm việc'\n- engine: OPA/Rego, Cedar cho policy-as-code tách khỏi business logic\n\nOTA: RBAC cho customer/agent/admin; ABAC cho 'agent hãng X chỉ thấy booking hãng X', 'refund &gt; ngưỡng cần cấp duyệt cao hơn'. [[o:Kiểm quyền ở server]] (gateway + service), không tin client; log mọi quyết định authorization nhạy cảm (audit).\n:::",
          "difficulty": "extra"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q7",
          "q": "API key vs OAuth token — khi nào dùng cái nào?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>API key</th><th>OAuth token</th></tr></thead><tbody>\n<tr><td>Định danh</td><td>ứng dụng/đối tác</td><td>user/scope</td></tr>\n<tr><td>Granularity</td><td>thấp</td><td>cao (scope)</td></tr>\n<tr><td>Thời hạn</td><td>tĩnh, dài hạn</td><td>ngắn hạn, revoke được</td></tr>\n<tr><td>Hợp cho</td><td>server-to-server đối tác</td><td>hành động thay mặt user</td></tr>\n</tbody></table>\n\n:::deep\n- [[b:API key]]: chuỗi bí mật định danh ứng dụng/đối tác (không phải user), dùng kèm IP allowlist + rate limit/quota. [[r:Rủi ro]]: tĩnh, dài hạn, lộ là dùng mãi → cần rotation + scope hạn chế\n- [[b:OAuth token]]: uỷ quyền theo user/scope, ngắn hạn, revoke được\n\nOTA/fintech: B2C → OAuth/OIDC; service nội bộ → mTLS + client-credentials; đối tác B2B → API key + mTLS + quota, hoặc client-credentials với scope.\n\nQuản API key: [[g:hash khi lưu]] (như mật khẩu), prefix để nhận diện, rotation không downtime (2 key cùng lúc khi xoay), audit. [[r:Không nhúng API key trong client/mobile]] (dễ trích xuất).\n:::",
          "difficulty": "extra"
        }
      ]
    },
    {
      "title": "Keycloak trong thực tế",
      "items": [
        {
          "id": "13-security-oauth2.keycloak-in-practice.q1",
          "difficulty": "core",
          "q": "Keycloak là gì, và realm, client cùng các endpoint cốt lõi là gì?",
          "a": "Keycloak là [[g:identity provider open-source]] (CNCF incubating từ 2023), hiện thực OAuth 2.x, OIDC và SAML 2.0. Nó đóng vai [[o:authorization server]] trong (13-security-oauth2.oauth2-oidc.q1) — để bạn không phải tự viết login, MFA, federation và phát hành token.\n\n**Mô hình đối tượng, từ ngoài vào trong:**\n\n<table class='cmp'><thead><tr><th>Khái niệm</th><th>Là gì</th><th>Bẫy</th></tr></thead><tbody>\n<tr><td><code>realm</code></td><td>Một tenant tách biệt: user, role, client và [[o:signing key]] riêng</td><td>Không có chuyện token realm này tin realm kia. <code>master</code> chỉ để quản trị Keycloak — đừng bao giờ đặt user ứng dụng ở đó</td></tr>\n<tr><td><code>client</code></td><td>Một ứng dụng đi xin token</td><td><code>public</code> (SPA/mobile, không secret, buộc PKCE) vs <code>confidential</code> (backend, có secret)</td></tr>\n<tr><td><code>client scope</code></td><td>Bó claim/role tái sử dụng được, thêm vào token</td><td>Scope <code>default</code> áp dụng âm thầm; <code>optional</code> chỉ khi client xin</td></tr>\n<tr><td><code>realm role</code> / <code>client role</code></td><td>Quyền toàn realm vs quyền theo từng client</td><td>Chúng nằm ở *hai* claim khác nhau — xem (13-security-oauth2.keycloak-in-practice.q3)</td></tr>\n<tr><td><code>identity provider</code></td><td>Federation lên trên (Google, Azure AD, LDAP)</td><td>Keycloak vẫn tự phát token của nó; token của upstream không phải cái API bạn thấy</td></tr>\n</tbody></table>\n\n**Mọi endpoint đều được discovery công bố** — đừng hardcode path:\n\n<pre><code><span class=\"c\">// một document cho biết mọi URL còn lại</span>\nGET /realms/{realm}/.well-known/openid-configuration\n<span class=\"c\">//</span>\nauthorization_endpoint  /realms/{r}/protocol/openid-connect/auth\ntoken_endpoint          /realms/{r}/protocol/openid-connect/token\njwks_uri                /realms/{r}/protocol/openid-connect/certs\nend_session_endpoint    /realms/{r}/protocol/openid-connect/logout\nintrospection_endpoint  /realms/{r}/protocol/openid-connect/token/introspect</code></pre>\n\n:::tip Cách trả lời phỏng vấn\nHãy nói Keycloak *thay thế* cái gì: nó là authorization server, nên service của bạn chỉ còn phần *validate* token — nửa resource server. Chính sự phân chia đó là lý do người ta chọn Keycloak.\n:::\n\n:::deep\n- **`issuer` phải khớp tuyệt đối**, từng ký tự, kể cả scheme, port và path đuôi. Keycloak nằm sau reverse proxy có sửa host sẽ phát token mà `iss` bị service từ chối — sửa bằng `KC_HOSTNAME` cộng `--proxy-headers=xforwarded`, không phải bằng cách nới lỏng validation.\n- Keycloak là stateful: cần database thật ([[r:không phải H2 của dev-mode]]) và nếu chạy cluster thì cần Infinispan cache cho session. Production chạy `start`, không phải `start-dev` — bản dev tắt yêu cầu HTTPS và dùng store in-memory.\n- `keycloak-spring-boot-starter` và các adapter cũ đã [[r:end-of-life]]. Service mới phải dùng Spring Security OAuth2 resource server thuần (13-security-oauth2.keycloak-in-practice.q2); phía resource server không còn gì riêng của Keycloak nữa.\n- Config realm phải nằm trong version control. Export rồi import khai báo (`kc.sh import`, hoặc `KeycloakRealmImport` của Keycloak Operator) để IdP tái tạo được, chứ không phải một bản dựng tay không ai lặp lại nổi.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q2",
          "difficulty": "advanced",
          "q": "Service Spring Boot validate token Keycloak thế nào, và phải kiểm những gì?",
          "a": "Service của bạn là [[g:resource server]]: nó không gọi Keycloak mỗi request. Nó lấy [[o:JWKS]] một lần, cache public key, rồi verify signature tại chỗ — đúng cơ chế ở (13-security-oauth2.jwt-tokens.q1).\n\n**Toàn bộ cấu hình gọn trong một property:**\n\n<pre><code><span class=\"c\"># chỉ issuer-uri là đủ — Spring đọc discovery và tự tìm JWKS</span>\nspring.security.oauth2.resourceserver.jwt.issuer-uri: \\\n  https://kc.example.com/realms/fintech\n<span class=\"c\">#</span>\n<span class=\"c\"># nó validate: signature, exp, nbf, iss — nhưng KHÔNG validate audience</span></code></pre>\n\n**`aud` không được kiểm mặc định, và đó chính là cái bẫy.** Mọi token cùng realm — kể cả token phát cho một client hoàn toàn khác — vẫn verify sạch. Phải thêm kiểm tra tường minh:\n\n<pre><code><span class=\"k\">@Bean</span>\nJwtDecoder <span class=\"f\">jwtDecoder</span>(OAuth2ResourceServerProperties p) {\n    String issuer = p.getJwt().getIssuerUri();\n    NimbusJwtDecoder decoder = JwtDecoders.<span class=\"f\">fromIssuerLocation</span>(issuer);\n    decoder.<span class=\"f\">setJwtValidator</span>(<span class=\"k\">new</span> DelegatingOAuth2TokenValidator&lt;&gt;(\n        JwtValidators.<span class=\"f\">createDefaultWithIssuer</span>(issuer),      <span class=\"c\">// sig, exp, iss</span>\n        <span class=\"k\">new</span> JwtClaimValidator&lt;List&lt;String&gt;&gt;(               <span class=\"c\">// cái còn thiếu</span>\n            <span class=\"s\">\"aud\"</span>, aud -&gt; aud != <span class=\"k\">null</span> &amp;&amp; aud.<span class=\"f\">contains</span>(<span class=\"s\">\"orders-api\"</span>))));\n    <span class=\"k\">return</span> decoder;\n}</code></pre>\n\nMuốn `aud` có trong token thì phía Keycloak phải gắn [[o:audience mapper]] cho client — mặc định Keycloak chỉ đặt client vào `azp`, nên `aud` có thể không tồn tại.\n\n:::warn Đừng bao giờ tin mấy claim này\nĐừng phân quyền theo `preferred_username` hay `email` — cả hai đều đổi được và `email` không nhất thiết đã verified. Hãy khoá dữ liệu theo [[r:`sub` bất biến]], đúng như chính app này làm với token Google (xem security model trong `CLAUDE.md`).\n:::\n\n:::deep\n- **Cache JWKS và rotate key.** Spring nạp lại key set khi thấy `kid` lạ, có rate limit (mặc định 5 phút) để một token rác không biến thành cơn bão JWKS. Rotate key ở Keycloak *trước khi* bỏ key cũ — nó vẫn serve cả hai nên token đang bay vẫn hợp lệ.\n- **Verify tại chỗ nghĩa là revoke không tức thì** — đúng trade-off ở (13-security-oauth2.jwt-tokens.q2).\n  - Cách thường dùng: access token sống ngắn (1–5 phút) cộng refresh token rotation.\n  - `introspect` cho revoke tức thì, đổi lại một network hop và Keycloak thành phụ thuộc cứng của mọi request.\n- **`realm_access` không phải authority của Spring.** Spring mặc định đọc `scope`/`scp`, nên `hasRole('ADMIN')` fail âm thầm cho tới khi bạn cấp converter (13-security-oauth2.keycloak-in-practice.q3).\n- Keycloak chỉ cần sống lúc startup nếu bạn tạo decoder eager; `JwtDecoders.fromIssuerLocation` gọi discovery ngay, nên Keycloak nguội là service không boot nổi. Khi thứ tự boot quan trọng, dùng `NimbusJwtDecoder.withJwkSetUri(...)` để fetch lazy.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q3",
          "difficulty": "advanced",
          "q": "Role của Keycloak nằm ở đâu trong token, và map sang authority của Spring thế nào?",
          "a": "Keycloak ghi role vào [[o:claim lồng nhau]] mà Spring Security không đọc, nên một integration mới toanh sẽ authenticate đúng rồi fail mọi bước authorization.\n\n**Trong token thực sự có gì:**\n\n<pre><code>{\n  <span class=\"s\">\"sub\"</span>: <span class=\"s\">\"8f14e45f-…\"</span>,           <span class=\"c\">// id user duy nhất ổn định</span>\n  <span class=\"s\">\"azp\"</span>: <span class=\"s\">\"orders-web\"</span>,           <span class=\"c\">// client nào đi xin</span>\n  <span class=\"s\">\"realm_access\"</span>: { <span class=\"s\">\"roles\"</span>: [<span class=\"s\">\"admin\"</span>, <span class=\"s\">\"teller\"</span>] },\n  <span class=\"s\">\"resource_access\"</span>: {              <span class=\"c\">// role theo từng client</span>\n    <span class=\"s\">\"orders-api\"</span>: { <span class=\"s\">\"roles\"</span>: [<span class=\"s\">\"orders:write\"</span>] }\n  },\n  <span class=\"s\">\"scope\"</span>: <span class=\"s\">\"openid profile email\"</span>   <span class=\"c\">// &lt;-- Spring CHỈ đọc cái này</span>\n}</code></pre>\n\n**Converter nối khoảng trống đó:**\n\n<pre><code><span class=\"k\">static</span> Collection&lt;GrantedAuthority&gt; <span class=\"f\">authorities</span>(Jwt jwt) {\n    Map&lt;String,Object&gt; realm = jwt.<span class=\"f\">getClaim</span>(<span class=\"s\">\"realm_access\"</span>);\n    <span class=\"k\">if</span> (realm == <span class=\"k\">null</span>) <span class=\"k\">return</span> List.<span class=\"f\">of</span>();\n    <span class=\"k\">var</span> roles = (Collection&lt;String&gt;) realm.<span class=\"f\">getOrDefault</span>(<span class=\"s\">\"roles\"</span>, List.<span class=\"f\">of</span>());\n    <span class=\"k\">return</span> roles.<span class=\"f\">stream</span>()\n        <span class=\"c\">// hasRole('ADMIN') tìm tiền tố ROLE_; thiếu nó thì chỉ</span>\n        <span class=\"c\">// hasAuthority('admin') chạy — một ca 403 âm thầm kinh điển</span>\n        .&lt;GrantedAuthority&gt;<span class=\"f\">map</span>(r -&gt; <span class=\"k\">new</span> SimpleGrantedAuthority(<span class=\"s\">\"ROLE_\"</span> + r.<span class=\"f\">toUpperCase</span>()))\n        .<span class=\"f\">toList</span>();\n}</code></pre>\n\n<table class='cmp'><thead><tr><th>Quyết định</th><th>Dùng</th></tr></thead><tbody>\n<tr><td>Quyền trải khắp mọi service</td><td><code>realm_access</code> — realm role</td></tr>\n<tr><td>Quyền chỉ có nghĩa với một API</td><td><code>resource_access</code> — client role, để <code>orders:write</code> không rò quyền sang service payments</td></tr>\n<tr><td>Cái *app client* được phép xin, không phải user là ai</td><td><code>scope</code> — uỷ quyền, không phải danh tính</td></tr>\n</tbody></table>\n\n:::deep\n- **Composite role** được bung lúc phát token, nên `manager` chứa `teller` sẽ về đủ cả hai. Tiện, và cũng là lý do token phình âm thầm — user thuộc nhiều group có thể đẩy JWT vượt giới hạn header 8 KB của proxy. Hãy giữ role thô trong token và resolve quyền chi tiết ở phía service.\n- **Group membership mặc định không có trong token.** Cần thì thêm group-membership mapper, nhưng nên map group sang role: group là cấu trúc tổ chức, role là hợp đồng phân quyền.\n- **Phân biệt `scope` với role là câu hỏi phỏng vấn thật:** [[g:scope là cái client được phép xin]], [[o:role là cái user đang là]].\n  - `scope=openid` cùng `realm_access.roles=[admin]` = một admin đang dùng app chỉ được cấp quyền đọc danh tính.\n  - Cả hai giới hạn đều áp, quyền hiệu dụng là phần giao.\n- Việc gì không tầm thường thì dùng `@PreAuthorize` trên method hơn là match URL pattern; mô hình vẫn là RBAC/ABAC ở (13-security-oauth2.designing-the-auth-system.q6), Keycloak chỉ cung cấp attribute.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q4",
          "difficulty": "advanced",
          "q": "Các service gọi nhau với Keycloak thế nào, và token exchange để làm gì?",
          "a": "Hai bài toán khác nhau, hai grant khác nhau — gộp chúng lại là lỗi thiết kế rất hay gặp.\n\n**Service tự đứng tên nó → `client_credentials`.** Không có user; `sub` của token chính là service account:\n\n<pre><code>POST /realms/fintech/protocol/openid-connect/token\nContent-Type: application/x-www-form-urlencoded\n<span class=\"c\">//</span>\ngrant_type=client_credentials\n&amp;client_id=batch-poster&amp;client_secret=…</code></pre>\n\n**Service đứng tên user → [[o:token exchange]]** (RFC 8693). Gateway giữ token của user; service phía sau cần một token phát cho chính nó nhưng vẫn mang danh tính user:\n\n<pre><code>grant_type=urn:ietf:params:oauth:grant-type:token-exchange\n&amp;subject_token=&lt;access token của bên gọi&gt;\n&amp;audience=ledger-api          <span class=\"c\">// phát cho service này</span>\n<span class=\"c\">// kết quả: token MỚI, aud=ledger-api, sub vẫn là user ban đầu</span></code></pre>\n\n<table class='cmp'><thead><tr><th>Cách</th><th>Khi nào đúng</th><th>Giá phải trả</th></tr></thead><tbody>\n<tr><td>Chuyển tiếp nguyên token</td><td>Mesh nội bộ tin nhau, một audience</td><td>[[r:Hop nào cũng replay được đi bất kỳ đâu]] — audience thành vô nghĩa</td></tr>\n<tr><td>Token exchange</td><td>Vượt ranh giới tin cậy mà vẫn giữ danh tính user</td><td>Một round-trip Keycloak mỗi hop (cache theo tuổi token)</td></tr>\n<tr><td><code>client_credentials</code></td><td>Batch job, cron, không có user</td><td>Mất danh tính user — audit log chỉ thấy tên service</td></tr>\n</tbody></table>\n\n:::warn Đừng chuyển tiếp token user vô hạn\nToken phát cho `orders-api` rồi đi qua bốn service thì service nào cũng replay được nó tới bất kỳ service nào trong realm. Hãy đặt `audience` thật và validate nó (13-security-oauth2.keycloak-in-practice.q2) — đó là thứ làm ranh giới có ý nghĩa.\n:::\n\n:::deep\n- **Token exchange phải bật theo từng client**, bị chặn bởi fine-grained admin permission: client đi exchange cần quyền tường minh trên audience đích.\n  - Tính năng này từng qua lại giữa \"preview\" và được hỗ trợ tuỳ version — [[o:hãy kiểm feature flag của đúng version bạn đang chạy]] thay vì đoán.\n- Với machine-to-machine thuần trong cluster, [[g:mTLS]] (13-security-oauth2.designing-the-auth-system.q2) có thể thay hẳn token: danh tính là certificate, không có gì để trộm hay replay. Dùng token khi cần danh tính *user* đi theo; dùng mTLS khi cần danh tính *service*.\n- Khi mức độ quan trọng đủ lớn, hãy buộc token vào client đang giữ nó: token bound bằng mTLS (RFC 8705) hoặc DPoP làm bearer token bị trộm trở nên vô dụng ở nơi khác.\n- Cache token service và refresh trước `exp`, tôn trọng `expires_in`. Service nào xin token mới cho từng outbound call sẽ biến Keycloak thành cổ chai của cả hệ thống.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q5",
          "difficulty": "extra",
          "q": "Chạy Keycloak trong production thì vỡ ở đâu?",
          "a": "Keycloak không phải sidecar stateless — nó là [[r:một single point of failure cho mọi lần login, có database và chạy cluster]]. Các kiểu vỡ đều thuộc vận hành, không phải khái niệm.\n\n<table class='cmp'><thead><tr><th>Kiểu vỡ</th><th>Nguyên nhân</th><th>Cách sửa</th></tr></thead><tbody>\n<tr><td><code>invalid_token</code>, lệch issuer</td><td>Proxy sửa host; <code>iss</code> không còn khớp cái service chờ đợi</td><td><code>KC_HOSTNAME</code> + <code>--proxy-headers=xforwarded</code>; đừng bao giờ \"sửa\" bằng cách bỏ validate</td></tr>\n<tr><td>Tự nhiên bị logout khi chạy cluster</td><td>Session nằm trong Infinispan; node chết mang theo bản duy nhất</td><td>Distributed cache với <code>owners</code> &gt; 1, sticky session ở LB</td></tr>\n<tr><td>Login chậm khi tải cao</td><td>Mỗi lần login đều ghi DB; pool connection cạn</td><td>Chỉnh size pool, theo dõi latency DB — Keycloak nhanh bằng database của nó</td></tr>\n<tr><td>Verify token đứng khắp nơi</td><td>Keycloak sập mà service lại fetch JWKS mỗi request</td><td>Cache JWKS (Spring mặc định đã cache); verify phải sống sót khi Keycloak chết</td></tr>\n<tr><td>Upgrade làm vỡ realm</td><td>Schema DB migrate tại chỗ, và chỉ đi một chiều</td><td>Backup DB, thử upgrade trên bản copy, pin tag image</td></tr>\n</tbody></table>\n\n**Câu nên nói ra:** Keycloak sập thì [[g:token đang có vẫn chạy]] (verify tại chỗ) nhưng [[r:không ai login hay refresh được]]. Sự bất đối xứng đó quyết định tuổi token — đủ dài để sống qua một lần restart, đủ ngắn để revoke còn có ý nghĩa.\n\n:::tip Sự thật về capacity\nLogin đắt (hash password cố tình chậm — bản mới mặc định Argon2), còn *verify* token miễn phí vì nó chạy trong service của bạn. Hãy size Keycloak theo tần suất login và refresh, không phải theo throughput request.\n:::\n\n:::deep\n- **Chạy HA, và coi DB là tầng quan trọng nhất:** tối thiểu hai replica, Postgres/MySQL thật có backup, PodDisruptionBudget, và readiness trên `/health/ready` (bản mới dùng management port 9000).\n  - Trên Kubernetes, Keycloak Operator làm phần lớn việc này (14-devops-k8s-best-practices.docker-kubernetes.q6).\n- **Bán kính thiệt hại.** Một realm dùng cho mọi ứng dụng nghĩa là một bản export realm sai làm sập toàn bộ login. Tách tenant ra realm riêng; `master` chỉ để quản trị.\n- **Observability** — cảnh báo theo tỷ lệ login fail (credential stuffing, xem (13-security-oauth2.designing-the-auth-system.q5)), p99 của token endpoint, độ bão hoà DB pool, và số node Infinispan.\n  - Keycloak xuất metric Micrometer; hãy đẩy vào cùng stack với (20-observability-sre.slos-alerting-incidents.q1).\n- **Đừng fork Keycloak để thêm tính năng.** Hãy dùng điểm mở rộng của nó (SPI, custom authenticator, event listener) — IdP đã fork là IdP không upgrade được, mà upgrade chính là đường nhận bản vá bảo mật.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q6",
          "difficulty": "extra",
          "q": "Keycloak vs IdP managed vs tự viết — lập luận chọn thế nào?",
          "a": "Người phỏng vấn hỏi câu này để nghe xem bạn có cân được [[o:chi phí vận hành với mức độ kiểm soát]] hay chỉ đọc tên món mình thích.\n\n<table class='cmp'><thead><tr><th></th><th>Keycloak (self-host)</th><th>Managed (Auth0/Cognito/Entra)</th><th>Tự viết</th></tr></thead><tbody>\n<tr><td>Dạng chi phí</td><td>Hạ tầng + thời gian ops của bạn; phẳng khi user tăng</td><td>Theo MAU — rẻ lúc đầu, đau khi lớn</td><td>Rẻ nhất lúc bắt đầu, đắt nhất lúc duy trì</td></tr>\n<tr><td>Dữ liệu ở đâu</td><td>[[g:Bạn giữ dữ liệu user]] — trong fintech thường là yếu tố quyết định</td><td>Bên thứ ba giữ; phải xem thuộc quyền tài phán nào</td><td>Bạn giữ, kể cả giữ luôn vụ rò rỉ</td></tr>\n<tr><td>Bạn phải vận hành</td><td>Một service stateful chạy cluster + DB (13-security-oauth2.keycloak-in-practice.q5)</td><td>Không gì cả</td><td>Hash password, MFA, revoke token, federation, và từng CVE</td></tr>\n<tr><td>Tuỳ biến</td><td>Sâu — SPI, custom flow, theme login riêng</td><td>Giới hạn trong khuôn vendor</td><td>Vô hạn, và hoàn toàn là việc của bạn</td></tr>\n<tr><td>Lock-in</td><td>Thấp: OIDC chuẩn, realm export được</td><td>Có thật: rule riêng và user store riêng</td><td>Không</td></tr>\n</tbody></table>\n\n**Lập trường bảo vệ được:** [[r:đừng bao giờ tự viết]] phần authentication — đó là đoạn code ít tạo khác biệt nhất mà bán kính thiệt hại lớn nhất.\n\n- Chọn **managed** khi team nhỏ và quy định dữ liệu cho phép.\n- Chọn **Keycloak** khi data residency, chi phí theo user, hoặc nhu cầu kiểm soát protocol là yếu tố quyết.\n\nCả hai đều giữ bạn ở OIDC chuẩn, nên code resource server ở (13-security-oauth2.keycloak-in-practice.q2) [[g:không đổi dù chọn đường nào]] — chính vì thế quyết định này đảo lại được.\n\n:::deep\n- Điểm đáng nói nhất là đường migration: vì cả hai nói OIDC, đổi IdP là đổi một `issuer-uri` và một converter role-claim, không phải đổi service. Cái *không* migrate êm là hash password (khác format — thường phải rehash ở lần login kế) và rule engine riêng của vendor.\n- Keycloak là [[o:Apache 2.0, không chia bản open-core]], nên không có tính năng nào bị dựng tường thu phí; giá phải trả là vận hành. Điều đó quan trọng khi so với sản phẩm tính tiền riêng cho tầng SSO hay audit log.\n- Nếu bạn muốn tính năng của Keycloak nhưng không muốn vận hành nó, Keycloak hosted (Red Hat build of Keycloak, hoặc nhà cung cấp managed) là đường giữa, và là câu trả lời tốt cho \"thế không có team ops thì sao?\".\n- Chọn gì thì [[g:security model vẫn là của bạn]]: validate `aud`, lấy `sub` bất biến làm khoá user, tuổi token ngắn, và authorization quyết ở service của bạn. IdP giúp bạn thuê ngoài authentication, không bao giờ thuê ngoài authorization.\n:::"
        }
      ]
    }
  ]
}
