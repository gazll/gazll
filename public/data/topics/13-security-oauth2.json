{
  "sections": [
    {
      "title": "OAuth2 & OIDC",
      "items": [
        {
          "id": "13-security-oauth2.oauth2-oidc.q1",
          "q": "OAuth 2.x: what are the roles, modern flows, and flows to avoid?",
          "a": "**Roles:** the [[b:resource owner]] grants access; the [[b:client]] requests it; the [[b:authorization server]] authenticates/authorizes and issues tokens; the [[b:resource server]] enforces the token for its API. OAuth is delegated [[g:authorization]], not a user-authentication protocol; OpenID Connect adds that layer.\n\nChoose a flow from the client and user context:\n\n- **Authorization Code + PKCE** for browser, native, and other user-facing clients. A server-side BFF can keep tokens away from browser JavaScript; a pure SPA remains a public client.\n- **Client Credentials** when a confidential workload acts as itself, not as a user. Prefer workload identity, private-key authentication, or federation over a long-lived shared secret.\n- **Device Authorization Grant** for input-constrained devices when supported.\n- **Refresh Token Grant** renews an existing authorization; issuance is optional and requires replay protection.\n- Avoid **Implicit** and **Resource Owner Password Credentials**. RFC 9700 recommends authorization-code responses and removes password handling from clients.\n\n:::deep\nOAuth 2.1 is still an [[o:Internet-Draft (draft-15 as reviewed in 2026)]], not an RFC. Use RFC 9700, the OAuth 2.0 Security BCP, as normative deployed guidance and track the draft for consolidation changes.\n\nScopes and audiences must be least-privilege and resource-specific. Client Credentials must not manufacture end-user identity; when service A calls B on behalf of a user, use an explicit delegation/token-exchange design with downscoped audience rather than blindly forwarding the original token.\n\nPasskeys/WebAuthn and MFA are authentication methods at the authorization server/identity provider, not OAuth grants. They strengthen how the user signs in while OAuth/OIDC governs tokens and delegation.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.oauth2-oidc.q2",
          "q": "How does Authorization Code + PKCE work, and why is PKCE needed?",
          "a": "The client creates a high-entropy `code_verifier`, derives an S256 `code_challenge`, stores verifier plus transaction state, and redirects the browser to the authorization endpoint.\n\nAfter authentication/consent, the authorization server sends a short-lived, one-time code to an exactly registered redirect URI. The client redeems that code over the token endpoint and proves possession of the verifier.\n\n<pre><code><span class=\"c\"># authorization request (line breaks only for readability)</span>\nGET /authorize?response_type=code&amp;client_id=web&amp;\n    code_challenge=BASE64URL(SHA256(verifier))&amp;code_challenge_method=S256&amp;\n    state=BOUND_TO_BROWSER_SESSION&amp;redirect_uri=https%3A%2F%2Fapp.example%2Fcallback\n<span class=\"c\">    </span>\n<span class=\"c\"># back-channel token request: the stolen code is useless without verifier</span>\nPOST /token\ngrant_type=authorization_code&amp;code=ONE_TIME_CODE&amp;code_verifier=ORIGINAL_VERIFIER</code></pre>\n\nPKCE prevents authorization-code interception and injection. RFC 9700 requires PKCE protection for public clients; confidential clients should also use it as defense in depth. The current OAuth 2.1 draft makes PKCE part of the default authorization-code flow, but OAuth 2.1 is [[r:not yet an RFC]].\n\n:::deep\nPKCE is one control in a transaction-bound flow:\n\n- use S256, high verifier entropy, one-time codes, short code lifetime, and exact redirect-URI matching;\n- bind `state` to the user-agent session for request/response correlation and CSRF defense; in OIDC also validate `nonce` in the ID token and the authorization-response issuer where applicable;\n- at token validation, check issuer and audience so a code/token from another authorization server or resource cannot be substituted;\n- do not put access tokens in redirect URLs.\n\nFor browser apps, prefer a BFF when the deployment can support one: the BFF is the confidential OAuth client, stores tokens server-side, and exposes a Secure/HttpOnly/SameSite session cookie with CSRF protection.\n\nA pure SPA uses Authorization Code + PKCE, minimizes token lifetime/storage, enforces a strong CSP, and assumes XSS can act with any token JavaScript can access.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.oauth2-oidc.q3",
          "q": "What's the difference between OAuth2 and OIDC?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>OAuth2</th><th>OIDC</th></tr></thead><tbody>\n<tr><td>Answers</td><td>'what is the app allowed to do'</td><td>'who is the user'</td></tr>\n<tr><td>Token</td><td>Access Token</td><td>+ ID Token (JWT, `sub`, claims)</td></tr>\n<tr><td>Purpose</td><td>authorization</td><td>authentication (+ authorization)</td></tr>\n</tbody></table>\n\nOIDC is built on top of OAuth2, adding the ID Token + the `/userinfo` endpoint. Modern SSO uses OIDC.\n\n:::deep\n- OAuth2 returns an *access token* (what the app is allowed to do) — it does NOT standardize saying who the user is\n- OIDC adds the ID Token (JWT: `sub`, `iss`, `aud`, `email`, `auth_time`), `/userinfo`, discovery via `/.well-known/openid-configuration`, and JWKS\n\n[[r:Common mistake]]: using the *access token* to determine user identity (wrong — the access token is for the resource server); using the ID token to call an API (wrong — the ID token is for the client to know who the user is).\n\nOTA: OIDC for B2C login (via Google/social IdP), access tokens for calling internal services with the appropriate scope.\n:::",
          "difficulty": "core"
        }
      ]
    },
    {
      "title": "JWT & tokens",
      "items": [
        {
          "id": "13-security-oauth2.jwt-tokens.q1",
          "q": "What is the JWT structure, and how is it signed and verified?",
          "a": "A commonly used signed JWT is the compact JWS form `header.payload.signature`, with each part base64url-encoded. The payload is [[r:not encrypted]] unless a separate JWE design is used.\n\n- Header: a pinned `alg`, a rotation key ID `kid`, and an explicit `typ` such as the access-token profile expected by the API.\n- Claims: `iss`, `sub`, `aud`, `exp`, `iat`, optionally `nbf`/`jti`, and authorization data such as scopes.\n- Signature: integrity/authenticity over the encoded header and payload using a trusted key.\n\n<pre><code><span class=\"c\">// Validation order; use a maintained JOSE/OAuth library, not home-grown crypto</span>\n<span class=\"f\">requireExpectedTypAndAllowedAlgorithm</span>(header);\nKey key = trustedJwks.<span class=\"f\">resolve</span>(header.kid());\nClaims c = jws.<span class=\"f\">verifyThenDecode</span>(key);\n<span class=\"f\">requireIssuer</span>(c, EXPECTED_ISSUER);\n<span class=\"f\">requireAudience</span>(c, THIS_API);\n<span class=\"f\">requireTimeWindow</span>(c.nbf(), c.exp(), ALLOWED_CLOCK_SKEW);\n<span class=\"f\">requireScopesAndSubjectPolicy</span>(c);</code></pre>\n\n:::warn Parsing is not validation\nDo not authorize from unverified claims. Reject `alg: none`, algorithm/key-type confusion, unexpected `typ`, untrusted `jku`/`x5u`, missing issuer/audience/time claims, and tokens intended for another API or token class. An ID token is not an API access token.\n:::\n\n:::deep\nJWKS handling is production logic: fetch only from issuer configuration you trust; cache keys; rate-limit refresh on an unknown `kid`; retain previous keys through the maximum token lifetime; and overlap old/new signing keys during rotation.\n\nA naïve “refetch on every unknown kid” path is a denial-of-service vector, while a cache that never refreshes breaks rotation.\n\n`typ`, issuer, audience, key set, and validation rules together prevent cross-JWT and token-substitution attacks. Signature validity says who signed bytes; authorization still has to check tenant, subject, scope/role, resource ownership, and current policy.\n\nKeep claims minimal, never put secrets in a readable payload, never log the token, and choose access-token lifetime from replay risk and revocation capability rather than a universal number of minutes.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.jwt-tokens.q2",
          "q": "JWT (stateless) vs opaque token / session — what are the trade-offs?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>JWT</th><th>Opaque + introspection</th><th>Session</th></tr></thead><tbody>\n<tr><td>Validate</td><td>locally (no call to the AS)</td><td>asks the AS every time</td><td>server-side state</td></tr>\n<tr><td>Revoke</td><td>hard (still valid until it expires)</td><td>immediate</td><td>easy</td></tr>\n<tr><td>Scale</td><td>good, low latency</td><td>extra round-trip</td><td>needs sticky/shared store</td></tr>\n</tbody></table>\n\n:::tip Bottom line\nJWT trades revocability for scale. Fintech typically uses: short-lived JWT access token + revocable refresh token + blacklist/rotation.\n:::\n\n:::deep\n- need immediate revocation (locking a fraudulent account) → short-lived access token + introspection for sensitive operations, or an opaque token\n- [[g:common hybrid pattern]]: JWT access token for 5–15 minutes (validated locally) + server-side refresh token *rotation* (revocable, reuse detection → revokes the whole family) + `jti` denylist for emergencies\n\n'JWT trades revocability for statelessness/scale; we compensate with a short TTL + revocation at the refresh layer' — [[r:avoid saying 'JWT is always better than sessions']].\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.jwt-tokens.q3",
          "q": "Access token vs refresh token, and what's the revocation strategy?",
          "a": "- An [[b:access token]] is presented to a resource server and should be audience-restricted, least-privilege, and short enough for the replay/revocation risk.\n- A [[b:refresh token]] is a higher-value credential used only at the authorization server. Issuance and lifetime are policy decisions; inactivity, consent, credential, device, or risk changes can terminate the grant early.\n\nFor public clients, RFC 9700 requires refresh tokens to be [[g:sender-constrained]] (for example DPoP) or rotated with reuse detection. Rotation issues a new token on every refresh, invalidates the predecessor, and revokes the active family when reuse reveals likely theft.\n\nConfidential clients must authenticate and the server must bind the token to that client.\n\nStorage follows the platform threat model: a BFF keeps OAuth tokens server-side and uses a Secure/HttpOnly/SameSite cookie plus CSRF defense; native apps use OS-protected storage; browser-only apps minimize token lifetime and commonly keep access tokens in memory.\n\nHttpOnly prevents JavaScript reads, not requests made by an XSS payload, so CSP and output safety still matter.\n\n:::deep\nRevocation is layered:\n\n- revoke the authorization/refresh-token family on logout, consent withdrawal, account compromise, credential reset, or reuse detection;\n- use opaque access tokens with introspection when immediate central revocation outweighs the availability/latency cost, or a short-lived signed token when local validation is the better trade-off;\n- use a `jti` denylist only for bounded emergency cases with an explicit retention/availability design;\n- require reauthentication or step-up with WebAuthn/MFA for high-risk operations, even when a token is valid;\n- consider DPoP or mTLS sender-constrained access tokens where bearer-token replay is a material risk.\n\nNever log tokens or URLs/headers containing them. Redact at ingestion, restrict security-log access, and alert on refresh reuse without recording the credential itself (20-observability-sre.slos-alerting-incidents.q4).\n:::",
          "difficulty": "core"
        }
      ]
    },
    {
      "title": "Designing the auth system",
      "items": [
        {
          "id": "13-security-oauth2.designing-the-auth-system.q1",
          "q": "How would you design the authentication/authorization system for microservices (SSO + gateway)?",
          "a": "Design three identities separately: the human/session, the OAuth client, and the calling workload.\n\n- A central [[b:authorization server/IdP]] handles OIDC login, WebAuthn/passkeys/MFA, consent, token issuance, keys, and session risk.\n- A [[b:gateway or BFF]] validates the external protocol at the edge, strips any client-supplied identity headers, and creates trusted internal context. A BFF keeps browser tokens server-side.\n- Each [[b:resource-owning service]] enforces scopes plus domain authorization such as tenant, object ownership, amount, and state transition. Edge validation alone cannot decide those invariants.\n- Workloads authenticate with short-lived federated identity, mTLS/SPIFFE, or an OAuth confidential-client method; avoid one shared static credential across services.\n\n:::tip Choose one internal token contract\nEither services validate an externally issued token with the exact issuer/audience profile, or a trusted edge exchanges it for a short-lived, downscoped internal token.\n:::\n\nDo not forward arbitrary identity headers or every original bearer token through all hops. Preserve the actor/delegation chain explicitly when a service acts for a user.\n:::deep\nValidation and authorization belong at different boundaries. The gateway can reject malformed/expired tokens, enforce coarse scopes, rate-limit, and translate protocols. The ledger still verifies that this subject may move money from this account and requires step-up for a high-risk action.\n\nFor local JWT validation, distribute trust through OIDC discovery/JWKS with bounded caches and rotation overlap. For opaque tokens, design introspection availability, caching, and revocation semantics.\n\nInternal calls need workload identity even when user context is present; one credential answers “which service?”, the delegated token answers “for which actor and authority?”\n\nPropagate an absolute deadline, trace context, and only the minimum identity attributes. Log authorization decisions and policy version without logging credentials or sensitive claims.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q2",
          "q": "What is mTLS, and how is it used for service-to-service authentication?",
          "a": "[[g:mTLS]] authenticates both TLS peers with certificates while encrypting the channel. It proves possession of a private key bound to an accepted certificate/trust domain; it does not by itself decide whether that workload may perform a business action.\n\nCommon uses:\n\n- workload-to-workload identity with certificates/SVIDs rotated by a mesh, workload-identity platform, or PKI automation;\n- OAuth client authentication and certificate-bound access tokens under RFC 8705, reducing replay from a different TLS client;\n- B2B partner APIs where both organizations can operate certificate issuance, renewal, revocation, and incident procedures.\n\nDPoP (RFC 9449) is an application-layer proof-of-possession option that can sender-constrain tokens without requiring the same TLS-client-certificate topology. Neither mechanism protects a fully compromised client that loses both token and key.\n\n:::deep\nA service mesh can automate issuance, rotation, and transport policy, but it is not “security with no code or operations.”\n\nDefine trust domains, namespace/service authorization, certificate lifetime, clock-skew behavior, CA/root rotation, revocation/compromise response, telemetry, and fail-open/fail-closed behavior.\n\nTest that plaintext bypass paths are closed.\n\nUse mTLS where the threat model and platform justify it; “every request must use a mesh” is not a universal architecture rule. It adds proxies or library integration, control-plane dependencies, latency/resource cost, and another policy layer.\n\nCloud workload federation or signed workload tokens may fit some boundaries better.\n\nKeep service identity and end-user delegation separate. An mTLS peer may be `booking-service`, while a scoped token says it is acting for user U on booking B. Authorization should evaluate both when both matter.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q3",
          "q": "How should OWASP Top 10:2025 and API Security Top 10:2023 shape backend controls?",
          "a": "Treat both lists as [[o:awareness and threat-modelling inputs]], not a compliance checklist.\n\nOWASP Top 10:2025 emphasizes broken access control, security misconfiguration, software supply-chain failures, cryptographic/injection/insecure-design/authentication failures, integrity, logging/alerting, and mishandled exceptional conditions.\n\nAPI Security Top 10:2023 adds API-specific focus such as object/function/property authorization, resource consumption, sensitive business flows, SSRF, inventory, and unsafe consumption of upstream APIs.\n\n<table class=\"cmp\"><thead><tr><th>Risk</th><th>Engineering controls</th></tr></thead><tbody>\n<tr><td>injection / XSS / CSRF</td><td>parameter binding; context-aware output encoding and CSP; SameSite plus CSRF tokens for cookie-authenticated writes</td></tr>\n<tr><td>BOLA/IDOR and function/property authorization</td><td>server-side tenant, object, field, and action checks on every path; deny by default; test negative cases</td></tr>\n<tr><td>resource and sensitive-flow abuse</td><td>payload/page limits, deadlines, quotas, bot/fraud controls, idempotency, and cost-aware admission</td></tr>\n<tr><td>SSRF / unsafe upstream APIs</td><td>destination allowlists, safe URL parsing, egress controls, response validation, timeouts,<br>and no blind redirect/credential forwarding</td></tr>\n<tr><td>supply chain and integrity</td><td>locked dependencies, SCA, SBOM, provenance, signed artifacts, isolated builds, and<br>signature/policy verification at deploy</td></tr>\n</tbody></table>\n\n:::deep\nMass assignment is prevented by explicit input DTOs/allowlists, not by hiding fields in the UI.\n\nError paths need the same authorization and data-minimization rules as success paths: fail closed where correctness/security requires it, return stable problem details, and avoid stack traces or secrets in responses.\n\nBuild a security test matrix from assets and abuse cases: cross-tenant IDs, role downgrade, hidden fields, large/deep payloads, race/replay, webhook SSRF, partner schema surprises, dependency compromise, key rotation, and unavailable policy/identity services.\n\nRun SAST/SCA/secret scanning early, DAST and authorization tests against a deployed system, then verify audit/alert signals through incident exercises.\n\nWAF and rate limiting are defense in depth, not substitutes for resource-level authorization, safe code, or business-flow anti-automation.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q4",
          "q": "How do you manage secrets properly?",
          "a": "Prefer [[g:no long-lived secret]] when workload identity or short-lived federation can provide the credential. When a secret is necessary, keep it out of source, build logs, images, CI variables with broad scope, and client-visible configuration.\n\n- store and issue it through a managed secret/KMS/HSM system with least-privilege identity and audited access;\n- inject at runtime using the platform channel whose leakage risks you understand (agent/file/volume/API; environment variables can appear in dumps or diagnostics);\n- separate environments and tenants, limit blast radius, and use short-lived dynamic credentials where supported;\n- rotate on compromise, personnel/ownership change, cryptoperiod, or policy trigger; automate renewal and test overlap/rollback rather than rotating blindly and causing an outage;\n- scan current code, Git history, artifacts, logs, tickets, and images; a discovered credential is revoked/reissued, not merely deleted from the latest commit.\n\n:::deep\nA Kubernetes `Secret` is base64-encoded data, not encryption by itself. Configure etcd encryption at rest, tight RBAC, namespace/service-account isolation, audit, and preferably an external secret/KMS integration for high-value material.\n\nJWT signing-key rotation requires publishing old and new public keys under distinct `kid` values, switching the signer, retaining the old verification key through maximum token lifetime plus skew, then retiring it.\n\nRoot/CA, database, webhook, PSP/GDS, and encryption-key rotations need similarly explicit dependency order and rollback.\n\nUse envelope encryption for stored data: a data-encryption key encrypts data and a KMS/HSM-managed key encrypts the DEK. Maintain an inventory of secret owner, consumers, scope, creation/expiry, rotation method, and emergency revocation.\n\nNever log the material; log the secret identifier/version and access decision.\n:::",
          "difficulty": "core"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q5",
          "q": "How do you protect auth endpoints from brute-force/credential stuffing?",
          "a": "- rate limit by IP + by account\n- exponential backoff/temporary lockout; CAPTCHA after N failed attempts\n- detect anomalies (login from an unusual location)\n- store passwords with a slow, salted hash function ([[g:bcrypt/argon2/scrypt]]), [[r:never plain MD5/SHA]]\n- MFA for sensitive operations\n- [[o:don't reveal 'whether the user exists']] through error messages\n\n:::deep\n- rate limit + lockout by IP *and* by account (defends against credential stuffing across many IPs against one account, &amp; brute-force against one account from one IP); exponential backoff; CAPTCHA past a threshold; anomaly detection (unfamiliar location/device → step-up MFA)\n- [[g:argon2id]] (recommended) / bcrypt / scrypt — NEVER plain MD5/SHA (fast, easy to brute-force on GPUs); don't leak 'user exists' via errors/timing (timing-safe)\n\nFintech/OTA: require phishing-resistant or step-up MFA where transaction risk, regulation and recovery policy demand it; prefer passkeys/WebAuthn where supported, and treat device/location signals as risk inputs rather than identity proof.\n\n[[r:Pitfall]]: lockout too aggressive → DoS against the real user (an attacker locks out the victim's account) → use CAPTCHA instead of a hard lockout, or lock by IP+account combined.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q6",
          "q": "RBAC vs ABAC — which authorization model should you choose?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>RBAC</th><th>ABAC</th></tr></thead><tbody>\n<tr><td>Basis</td><td>roles (admin, teller)</td><td>attributes (user, resource, context, time)</td></tr>\n<tr><td>Pros</td><td>simple, easy to manage/audit</td><td>flexible for complex rules</td></tr>\n<tr><td>Cons</td><td>role explosion as rules get complex</td><td>harder to debug/audit</td></tr>\n</tbody></table>\n\nMany systems combine both: RBAC as the base + ABAC for fine-grained rules.\n\n:::deep\n- ABAC example: 'an agent may only approve bookings below their branch's limit during working hours'\n- engines: OPA/Rego, Cedar for policy-as-code decoupled from business logic\n\nOTA: RBAC for customer/agent/admin; ABAC for 'agent from airline X only sees airline X's bookings', 'refunds above a threshold need higher-level approval'. [[o:Check permissions server-side]] (gateway + service), never trust the client; log every sensitive authorization decision (audit).\n:::",
          "difficulty": "extra"
        },
        {
          "id": "13-security-oauth2.designing-the-auth-system.q7",
          "q": "API key vs OAuth token — when do you use which?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>API key</th><th>OAuth token</th></tr></thead><tbody>\n<tr><td>Identifies</td><td>an application/partner</td><td>a user/scope</td></tr>\n<tr><td>Granularity</td><td>low</td><td>high (scope)</td></tr>\n<tr><td>Lifetime</td><td>static, long-lived</td><td>short-lived, revocable</td></tr>\n<tr><td>Good for</td><td>partner server-to-server</td><td>acting on behalf of a user</td></tr>\n</tbody></table>\n\n:::deep\n- [[b:API key]]: a secret string identifying an application/partner (not a user), used alongside an IP allowlist + rate limit/quota. [[r:Risk]]: static, long-lived, once leaked it stays usable indefinitely → needs rotation + a restricted scope\n- [[b:OAuth token]]: authorization scoped to a user/scope, short-lived, revocable\n\nOTA/fintech: B2C → OAuth/OIDC; internal services → mTLS + client-credentials; B2B partners → API key + mTLS + quota, or client-credentials with scope.\n\nManaging API keys: [[g:hash them at rest]] (like a password), use a prefix for identification, rotate without downtime (two keys active simultaneously during rotation), audit. [[r:Never embed an API key in a client/mobile app]] (too easy to extract).\n:::",
          "difficulty": "extra"
        }
      ]
    },
    {
      "title": "Keycloak in practice",
      "items": [
        {
          "id": "13-security-oauth2.keycloak-in-practice.q1",
          "difficulty": "core",
          "q": "What is Keycloak, and what are realms, clients and its core endpoints?",
          "a": "Keycloak is an [[g:open-source identity provider]] (CNCF incubating since 2023) implementing OAuth 2.x, OIDC and SAML 2.0. It plays the [[o:authorization server]] role from (13-security-oauth2.oauth2-oidc.q1) so you do not build login, MFA, federation and token issuance yourself.\n\n**The object model, from the outside in:**\n\n<table class='cmp'><thead><tr><th>Concept</th><th>What it is</th><th>Gotcha</th></tr></thead><tbody>\n<tr><td><code>realm</code></td><td>An isolated tenant: its own users, roles, clients and [[o:signing keys]]</td><td>Cross-realm token trust does not exist. <code>master</code> is for administering Keycloak — never put application users there</td></tr>\n<tr><td><code>client</code></td><td>One application asking for tokens</td><td><code>public</code> (SPA/mobile, no secret, PKCE required) vs <code>confidential</code> (backend, has a secret)</td></tr>\n<tr><td><code>client scope</code></td><td>Reusable bundle of claims/roles added to a token</td><td>Default scopes apply silently; <code>optional</code> ones only when requested</td></tr>\n<tr><td><code>realm role</code> / <code>client role</code></td><td>Realm-wide vs per-client authority</td><td>They land in *different* token claims — see (13-security-oauth2.keycloak-in-practice.q3)</td></tr>\n<tr><td><code>identity provider</code></td><td>Upstream federation (Google, Azure AD, LDAP)</td><td>Keycloak still mints its own token; the upstream one is not what your API sees</td></tr>\n</tbody></table>\n\n**Every endpoint is advertised by discovery** — never hardcode paths:\n\n<pre><code><span class=\"c\">// one document tells you every other URL</span>\nGET /realms/{realm}/.well-known/openid-configuration\n<span class=\"c\">//</span>\nauthorization_endpoint  /realms/{r}/protocol/openid-connect/auth\ntoken_endpoint          /realms/{r}/protocol/openid-connect/token\njwks_uri                /realms/{r}/protocol/openid-connect/certs\nend_session_endpoint    /realms/{r}/protocol/openid-connect/logout\nintrospection_endpoint  /realms/{r}/protocol/openid-connect/token/introspect</code></pre>\n\n:::tip Interview framing\nSay what Keycloak *replaces*: it is the authorization server, so your services keep only token *validation* — the resource-server half. That split is the whole reason to adopt it.\n:::\n\n:::deep\n- **`issuer` must match exactly**, character for character, including scheme, port and any trailing path. A Keycloak behind a reverse proxy that rewrites the host mints tokens whose `iss` your service then rejects — fix it with `KC_HOSTNAME` plus `--proxy-headers=xforwarded`, not by loosening validation.\n- Keycloak is stateful: it needs a real database ([[r:not the dev-mode H2]]) and, for a cluster, an Infinispan cache for sessions. Run `start` and not `start-dev` in production — the latter disables HTTPS requirements and uses an in-memory store.\n- The legacy `keycloak-spring-boot-starter` and its adapters are [[r:end-of-life]]. New services must use plain Spring Security OAuth2 resource server (13-security-oauth2.keycloak-in-practice.q2); there is nothing Keycloak-specific left on the resource-server side.\n- Realm config belongs in version control. Export it and import declaratively (`kc.sh import`, or the Keycloak Operator's `KeycloakRealmImport`) so the IdP is reproducible rather than a hand-clicked snowflake.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q2",
          "difficulty": "advanced",
          "q": "How does a Spring Boot service validate a Keycloak token, and what must you verify?",
          "a": "Your service is a [[g:resource server]]: it never talks to Keycloak per request. It fetches the [[o:JWKS]] once, caches the public keys, and verifies signatures locally — the mechanism from (13-security-oauth2.jwt-tokens.q1).\n\n**The whole configuration is one property:**\n\n<pre><code><span class=\"c\"># issuer-uri alone is enough — Spring reads discovery and finds the JWKS</span>\nspring.security.oauth2.resourceserver.jwt.issuer-uri: \\\n  https://kc.example.com/realms/fintech\n<span class=\"c\">#</span>\n<span class=\"c\"># it validates: signature, exp, nbf, and iss — but NOT audience</span></code></pre>\n\n**`aud` is not checked by default, and that is the trap.** Any token from the same realm — including one minted for a completely different client — verifies fine. Add the check explicitly:\n\n<pre><code><span class=\"k\">@Bean</span>\nJwtDecoder <span class=\"f\">jwtDecoder</span>(OAuth2ResourceServerProperties p) {\n    String issuer = p.getJwt().getIssuerUri();\n    NimbusJwtDecoder decoder = JwtDecoders.<span class=\"f\">fromIssuerLocation</span>(issuer);\n    decoder.<span class=\"f\">setJwtValidator</span>(<span class=\"k\">new</span> DelegatingOAuth2TokenValidator&lt;&gt;(\n        JwtValidators.<span class=\"f\">createDefaultWithIssuer</span>(issuer),      <span class=\"c\">// sig, exp, iss</span>\n        <span class=\"k\">new</span> JwtClaimValidator&lt;List&lt;String&gt;&gt;(               <span class=\"c\">// the missing one</span>\n            <span class=\"s\">\"aud\"</span>, aud -&gt; aud != <span class=\"k\">null</span> &amp;&amp; aud.<span class=\"f\">contains</span>(<span class=\"s\">\"orders-api\"</span>))));\n    <span class=\"k\">return</span> decoder;\n}</code></pre>\n\nGetting `aud` into the token at all takes a Keycloak-side [[o:audience mapper]] on the client — by default Keycloak puts the client only in `azp`, so `aud` may be absent entirely.\n\n:::warn Never trust these\nDo not authorize on `preferred_username` or `email` — both are mutable and `email` need not be verified. Bind rows to the [[r:immutable `sub`]], exactly as the local app does with the Google token (see the security model in `CLAUDE.md`).\n:::\n\n:::deep\n- **JWKS caching and key rotation.** Spring refreshes the key set when it sees an unknown `kid`, with a rate limit (default 5 minutes) so a bad token cannot turn into a JWKS stampede. Rotate keys in Keycloak *before* retiring the old one — it keeps serving both so in-flight tokens stay valid.\n- **Local verification means revocation is not immediate** — the same trade-off as (13-security-oauth2.jwt-tokens.q2).\n  - Usual answer: short access-token lifetimes (1–5 min) plus refresh-token rotation.\n  - `introspect` gives instant revocation, at the cost of a network hop and a hard Keycloak dependency on every request.\n- **`realm_access` is not a Spring authority.** Spring reads `scope`/`scp` by default, so `hasRole('ADMIN')` fails silently until you supply a converter (13-security-oauth2.keycloak-in-practice.q3).\n- Keycloak must be reachable at startup only if you build the decoder eagerly; `JwtDecoders.fromIssuerLocation` fetches discovery immediately, which makes a cold Keycloak a boot failure. Use `NimbusJwtDecoder.withJwkSetUri(...)` for lazy fetching when boot ordering matters.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q3",
          "difficulty": "advanced",
          "q": "Where do Keycloak roles land in the token, and how do you map them to Spring authorities?",
          "a": "Keycloak writes roles into [[o:nested claims]] that Spring Security does not read, so a fresh integration authenticates correctly and then fails every authorization check.\n\n**What is actually in the token:**\n\n<pre><code>{\n  <span class=\"s\">\"sub\"</span>: <span class=\"s\">\"8f14e45f-…\"</span>,           <span class=\"c\">// the only stable user id</span>\n  <span class=\"s\">\"azp\"</span>: <span class=\"s\">\"orders-web\"</span>,           <span class=\"c\">// which client asked</span>\n  <span class=\"s\">\"realm_access\"</span>: { <span class=\"s\">\"roles\"</span>: [<span class=\"s\">\"admin\"</span>, <span class=\"s\">\"teller\"</span>] },\n  <span class=\"s\">\"resource_access\"</span>: {              <span class=\"c\">// per-client roles</span>\n    <span class=\"s\">\"orders-api\"</span>: { <span class=\"s\">\"roles\"</span>: [<span class=\"s\">\"orders:write\"</span>] }\n  },\n  <span class=\"s\">\"scope\"</span>: <span class=\"s\">\"openid profile email\"</span>   <span class=\"c\">// &lt;-- Spring reads ONLY this</span>\n}</code></pre>\n\n**The converter that bridges the gap:**\n\n<pre><code><span class=\"k\">static</span> Collection&lt;GrantedAuthority&gt; <span class=\"f\">authorities</span>(Jwt jwt) {\n    Map&lt;String,Object&gt; realm = jwt.<span class=\"f\">getClaim</span>(<span class=\"s\">\"realm_access\"</span>);\n    <span class=\"k\">if</span> (realm == <span class=\"k\">null</span>) <span class=\"k\">return</span> List.<span class=\"f\">of</span>();\n    <span class=\"k\">var</span> roles = (Collection&lt;String&gt;) realm.<span class=\"f\">getOrDefault</span>(<span class=\"s\">\"roles\"</span>, List.<span class=\"f\">of</span>());\n    <span class=\"k\">return</span> roles.<span class=\"f\">stream</span>()\n        <span class=\"c\">// hasRole('ADMIN') looks for the ROLE_ prefix; omit it and</span>\n        <span class=\"c\">// only hasAuthority('admin') works — a classic silent 403</span>\n        .&lt;GrantedAuthority&gt;<span class=\"f\">map</span>(r -&gt; <span class=\"k\">new</span> SimpleGrantedAuthority(<span class=\"s\">\"ROLE_\"</span> + r.<span class=\"f\">toUpperCase</span>()))\n        .<span class=\"f\">toList</span>();\n}</code></pre>\n\n<table class='cmp'><thead><tr><th>Decision</th><th>Use</th></tr></thead><tbody>\n<tr><td>Authority spanning every service</td><td><code>realm_access</code> — a realm role</td></tr>\n<tr><td>Permission meaningful to one API only</td><td><code>resource_access</code> — a client role, so <code>orders:write</code> cannot leak authority into the payments service</td></tr>\n<tr><td>What the *client app* may ask for, not what the user is</td><td><code>scope</code> — delegation, not identity</td></tr>\n</tbody></table>\n\n:::deep\n- **Composite roles** expand at token-mint time, so `manager` containing `teller` arrives as both. Convenient, and the reason tokens quietly grow — a user in many groups can push the JWT past an 8 KB proxy header limit. Keep coarse roles in the token and resolve fine-grained permissions service-side.\n- **Group membership is not in the token by default.** Add a group-membership mapper if you need it, but prefer mapping groups to roles: groups are an organisational structure, roles are the authorization contract.\n- **`scope` vs role is a real interview distinction:** [[g:scope is what the client may request]], [[o:role is what the user is]].\n  - `scope=openid` with `realm_access.roles=[admin]` = an admin using an app granted only identity access.\n  - Both limits apply, and the effective permission is the intersection.\n- Prefer `@PreAuthorize` on the method over URL-pattern matching for anything non-trivial; the model is RBAC/ABAC from (13-security-oauth2.designing-the-auth-system.q6), and Keycloak only supplies the attributes.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q4",
          "difficulty": "advanced",
          "q": "How do services call each other with Keycloak, and what is token exchange for?",
          "a": "Two different problems, two different grants — conflating them is a common design error.\n\n**Service acting as itself → `client_credentials`.** There is no user; the token's `sub` is the service account:\n\n<pre><code>POST /realms/fintech/protocol/openid-connect/token\nContent-Type: application/x-www-form-urlencoded\n<span class=\"c\">//</span>\ngrant_type=client_credentials\n&amp;client_id=batch-poster&amp;client_secret=…</code></pre>\n\n**Service acting on behalf of a user → [[o:token exchange]]** (RFC 8693). The gateway holds the user's token; the downstream service needs one scoped to itself but still carrying the user's identity:\n\n<pre><code>grant_type=urn:ietf:params:oauth:grant-type:token-exchange\n&amp;subject_token=&lt;the caller's access token&gt;\n&amp;audience=ledger-api          <span class=\"c\">// mint for this service</span>\n<span class=\"c\">// result: a NEW token, aud=ledger-api, sub still the original user</span></code></pre>\n\n<table class='cmp'><thead><tr><th>Approach</th><th>When it is right</th><th>Cost</th></tr></thead><tbody>\n<tr><td>Forward the same token</td><td>Trusted internal mesh, one audience</td><td>[[r:Any hop can replay it anywhere]] — audience is meaningless</td></tr>\n<tr><td>Token exchange</td><td>Crossing a trust boundary while keeping user identity</td><td>A Keycloak round-trip per hop (cache it for the token's lifetime)</td></tr>\n<tr><td><code>client_credentials</code></td><td>Batch jobs, cron, no user involved</td><td>Loses user identity — audit logs show only the service</td></tr>\n</tbody></table>\n\n:::warn Do not forward a user token indefinitely\nA token minted for `orders-api` and forwarded through four services can be replayed by any of them against any service in the realm. Set a real `audience` and validate it (13-security-oauth2.keycloak-in-practice.q2) — that is what makes the boundary mean something.\n:::\n\n:::deep\n- **Token exchange is opt-in per client**, gated by fine-grained admin permissions: the exchanging client needs explicit authority over the target audience.\n  - It has moved between \"preview\" and supported across versions — [[o:check the feature flags for the version you actually run]] rather than assuming.\n- For pure machine-to-machine inside a cluster, [[g:mTLS]] (13-security-oauth2.designing-the-auth-system.q2) may replace tokens entirely: the identity is the certificate and there is nothing to steal or replay. Use tokens when you need *user* identity to travel; use mTLS when you need *service* identity.\n- Bind the token to the client that holds it where the stakes justify it: mTLS-bound tokens (RFC 8705) or DPoP make a stolen bearer token useless elsewhere.\n- Cache service tokens and refresh before `exp`, honouring `expires_in`. A service that requests a fresh token per outbound call turns Keycloak into the bottleneck for the entire system.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q5",
          "difficulty": "extra",
          "q": "What breaks when you run Keycloak in production?",
          "a": "Keycloak is not a stateless sidecar — it is a [[r:database-backed, clustered, single point of failure for every login]]. The failure modes are operational, not conceptual.\n\n<table class='cmp'><thead><tr><th>Failure</th><th>Cause</th><th>Fix</th></tr></thead><tbody>\n<tr><td><code>invalid_token</code>, issuer mismatch</td><td>Proxy rewrites the host; <code>iss</code> no longer matches what services expect</td><td><code>KC_HOSTNAME</code> + <code>--proxy-headers=xforwarded</code>; never \"fix\" it by skipping validation</td></tr>\n<tr><td>Random logouts in a cluster</td><td>Sessions live in Infinispan; a node dies with the only copy</td><td>Distributed cache with <code>owners</code> &gt; 1, sticky sessions at the LB</td></tr>\n<tr><td>Slow logins under load</td><td>Every login writes to the DB; connection pool exhausted</td><td>Size the pool, watch DB latency — Keycloak is as fast as its database</td></tr>\n<tr><td>Token verification stalls everywhere</td><td>Keycloak down and services fetch JWKS per request</td><td>Cache the JWKS (default in Spring); verification must survive a Keycloak outage</td></tr>\n<tr><td>Upgrade breaks the realm</td><td>DB schema migrates in place, and it is one-way</td><td>Back up the DB, test the upgrade on a copy, pin the image tag</td></tr>\n</tbody></table>\n\n**The thing to say out loud:** if Keycloak is down, [[g:existing tokens keep working]] (local verification) but [[r:nobody can log in or refresh]]. That asymmetry decides your token lifetimes — long enough to ride out a restart, short enough that revocation means something.\n\n:::tip Capacity reality\nLogin is expensive (password hashing is deliberately slow — Argon2 by default in recent versions), token *verification* is free because it happens in your service. Size Keycloak for the login and refresh rate, not for request throughput.\n:::\n\n:::deep\n- **Run it HA, and treat the DB as the tier that matters:** at least two replicas, a real Postgres/MySQL with backups, PodDisruptionBudget, and readiness on `/health/ready` (management port 9000 in recent versions).\n  - The Keycloak Operator handles most of this on Kubernetes (14-devops-k8s-best-practices.docker-kubernetes.q6).\n- **Blast radius.** One realm for every application means one bad realm export takes down every login. Separate tenants into separate realms; keep `master` for administration only.\n- **Observability** — alert on login failure rate (credential stuffing, see (13-security-oauth2.designing-the-auth-system.q5)), token endpoint p99, DB pool saturation, and Infinispan cluster size.\n  - Keycloak exposes Micrometer metrics; feed them into the same stack as (20-observability-sre.slos-alerting-incidents.q1).\n- **Do not fork Keycloak to add a feature.** Use its extension points (SPIs, custom authenticators, event listeners) — a forked IdP is unupgradable, and upgrades are how you get security fixes.\n:::"
        },
        {
          "id": "13-security-oauth2.keycloak-in-practice.q6",
          "difficulty": "extra",
          "q": "Keycloak vs a managed IdP vs rolling your own — how do you argue the choice?",
          "a": "Interviewers ask this to hear whether you can weigh [[o:operational cost against control]] instead of naming a favourite.\n\n<table class='cmp'><thead><tr><th></th><th>Keycloak (self-hosted)</th><th>Managed (Auth0/Cognito/Entra)</th><th>Build it yourself</th></tr></thead><tbody>\n<tr><td>Cost shape</td><td>Infra + your ops time; flat as users grow</td><td>Per-MAU — cheap early, painful at scale</td><td>Cheapest to start, most expensive to keep</td></tr>\n<tr><td>Data residency</td><td>[[g:You hold the user data]] — often the deciding factor in fintech</td><td>Third party holds it; check the jurisdiction</td><td>You hold it, including the breach</td></tr>\n<tr><td>You now operate</td><td>A clustered stateful service + DB (13-security-oauth2.keycloak-in-practice.q5)</td><td>Nothing</td><td>Password hashing, MFA, token revocation, federation, and every CVE</td></tr>\n<tr><td>Customisation</td><td>Deep — SPIs, custom flows, own login theme</td><td>Bounded by the vendor</td><td>Unlimited, and entirely your problem</td></tr>\n<tr><td>Lock-in</td><td>Low: standard OIDC, exportable realm</td><td>Real: proprietary rules and user store</td><td>None</td></tr>\n</tbody></table>\n\n**The defensible position:** [[r:never build your own]] for authentication — it is the least differentiating, highest-blast-radius code you could write.\n\n- Choose **managed** when the team is small and data residency permits.\n- Choose **Keycloak** when residency, per-user cost, or protocol control decide it.\n\nBoth keep you on standard OIDC, so the resource-server code in (13-security-oauth2.keycloak-in-practice.q2) is [[g:unchanged either way]] — which is exactly why the decision is reversible.\n\n:::deep\n- The migration path is the point worth making: because both speak OIDC, switching IdP changes an `issuer-uri` and a role-claim converter, not your services. What does *not* migrate cleanly is password hashes (formats differ — usually a rehash-on-next-login shim) and the vendor-specific rules engine.\n- Keycloak is [[o:Apache 2.0 with no open-core split]], so there is no feature paywall; the cost is the operations. That matters when comparing against products whose SSO or audit-log tier is priced separately.\n- If you take Keycloak for its features but not its operations, hosted Keycloak (Red Hat build of Keycloak, or a managed provider) is the middle path and a good answer to \"what if we lack an ops team?\".\n- Whatever you choose, the [[g:security model stays yours]]: `aud` validation, immutable `sub` as the user key, short token lifetimes, and authorization decided in your service. An IdP outsources authentication, never authorization.\n:::"
        }
      ]
    }
  ]
}
