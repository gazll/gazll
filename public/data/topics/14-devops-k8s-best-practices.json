{
  "sections": [
    {
      "title": "Docker & Kubernetes",
      "items": [
        {
          "id": "14-devops-k8s-best-practices.docker-kubernetes.q1",
          "q": "Docker: image layers, multi-stage builds, and common commands?",
          "a": "An image is made up of stacked [[b:layers]] (each Dockerfile instruction creates one cacheable layer).\n\n[[g:Multi-stage build]]: a build stage (Maven/JDK) produces the artifact; the runtime stage copies only that artifact into a minimal base image (JRE/distroless) → a smaller image with fewer vulnerabilities.\n\n<pre><code><span class=\"k\">FROM</span> maven <span class=\"k\">AS</span> build\n<span class=\"k\">COPY</span> . . &amp;&amp; mvn package\n<span class=\"k\">FROM</span> eclipse-temurin:<span class=\"n\">21</span>-jre\n<span class=\"k\">COPY</span> --from=build /app/target/app.jar /app.jar\n<span class=\"k\">ENTRYPOINT</span> [<span class=\"s\">\"java\"</span>,<span class=\"s\">\"-jar\"</span>,<span class=\"s\">\"/app.jar\"</span>]</code></pre>\n\nCommon commands: `docker build -t`, `run -d -p`, `ps`, `logs -f`, `exec -it`, `image prune`.\n\n[[o:Cache optimization]]: copy `pom.xml` and download dependencies before copying the source.\n\n:::deep\n- each instruction creates an immutable layer cached by content → put the least frequently changed parts first (copy `pom.xml`/lockfile and download dependencies), then copy the source → a code change does not force dependencies to be downloaded again\n- multi-stage: use a heavyweight build stage (JDK+Maven) → copy only the JAR into the runtime stage (JRE slim/distroless) → a smaller image, fewer CVEs, and no build tools (a smaller attack surface)\n- a smaller image pulls faster (so rollouts are faster) and has fewer vulnerabilities; distroless images can omit the shell, while Alpine keeps only a minimal userland (safer and smaller, but harder to debug)\n\n[[r:Pitfall]]: a secret placed in a layer remains in the image history even if a later layer deletes it → use build secrets or a multi-stage build. Pin the base image by digest for reproducible builds. For Java, use Spring Boot layered JARs to separate the dependency layer.\n:::",
          "translated": true,
          "difficulty": "core"
        },
        {
          "id": "14-devops-k8s-best-practices.docker-kubernetes.q2",
          "q": "Kubernetes architecture: control plane, nodes, and pods?",
          "a": "- [[b:Control plane]]: API server (entry point), etcd (state store), scheduler (places pods), controller manager (reconciles desired vs. actual state)\n- [[b:Node]]: kubelet (runs pods), kube-proxy (networking), container runtime\n- [[b:Pod]]: the smallest deployable unit (1+ containers sharing networking and volumes)\n\nKubernetes operates through [[g:declarative configuration + reconciliation]]: you declare the desired state, and controllers continuously drive the system toward it.\n\n:::deep\n- control plane: `api-server` (the single entry point, authentication/admission), `etcd` (a Raft KV store—the source of truth), `scheduler` (assigns pods according to resources/affinity), `controller-manager` (the 'desired vs actual' control loop)\n- node: `kubelet` (runs pods), `kube-proxy` (Service networking), containerd; a pod is 1+ containers sharing networking and volumes (for example, a sidecar)\n- [[g:reconciliation loop]]: provides self-healing (a pod dies → another is created); this explains why 'delete the pod and it comes back → update the Deployment instead'\n\netcd is mission-critical (backups plus an odd-numbered HA quorum). For an OTA platform, run stateless applications in Deployments; run databases/Kafka in StatefulSets or as managed services outside the cluster.\n:::",
          "translated": true,
          "difficulty": "core"
        },
        {
          "id": "14-devops-k8s-best-practices.docker-kubernetes.q3",
          "q": "Deployment vs. StatefulSet—what differs, and when should each be used?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>Deployment</th><th>StatefulSet</th></tr></thead><tbody>\n<tr><td>Workload</td><td>stateless applications</td><td>stateful systems (DB, Kafka, Zookeeper)</td></tr>\n<tr><td>Pod identity</td><td>arbitrary (cattle)</td><td>stable: pod-0, pod-1 (pets)</td></tr>\n<tr><td>Storage</td><td>not permanently bound</td><td>a dedicated PVC per pod</td></tr>\n<tr><td>Scaling/startup</td><td>flexible</td><td>ordered</td></tr>\n</tbody></table>\n\n:::tip Rule of thumb\nStateless workload → Deployment; stable per-pod identity and durable storage → StatefulSet. Web applications are usually stateless and keep state in a database or cache.\n:::\n\n:::deep\n- StatefulSet: stable names `name-0..N`, dedicated PVCs through `volumeClaimTemplates` (data survives rescheduling), a headless Service that provides DNS such as `pod-0.svc`, and ordered scaling/rolling updates\n- [[r:Pitfall]]: putting a stateless application in a StatefulSet 'just to be safe' sacrifices scaling flexibility; putting a database in a Deployment loses the pod-to-volume association\n\nThe concise distinction is '[[g:cattle vs pets]]': Deployment pods are cattle; StatefulSet pods are named pets.\n:::",
          "translated": true,
          "difficulty": "hard"
        },
        {
          "id": "14-devops-k8s-best-practices.docker-kubernetes.q4",
          "q": "What are the most useful kubectl commands, and how do you 'deploy a new pod' or perform a rolling update?",
          "a": "Common commands include `kubectl get/describe`, `logs -f`, `exec -it`, `apply -f`, `rollout status/undo`, `scale`, and `port-forward`.\n\n**Deploy a new version** declaratively: update the image in the manifest → run `kubectl apply -f` (or `set image`) → the Deployment performs a [[g:rolling update]] by creating a new ReplicaSet and gradually scaling new pods up and old pods down according to maxSurge/maxUnavailable.\n\nOn failure, run `kubectl rollout undo`. [[r:Avoid manually running `kubectl edit` in production]] because it creates drift from Git.\n\n:::deep\n- [[r:DO NOT use `kubectl run` to create a bare pod]]; it cannot self-heal. Update the Deployment's image tag instead → Kubernetes performs the rolling update automatically\n- use `set image` or `apply -f` (GitOps favors apply); use `rollout status`/`undo` to monitor the rollout or roll back to the previous ReplicaSet; `maxSurge`/`maxUnavailable` plus a readiness probe govern a zero-downtime rolling update\n\n[[r:Pitfall]]: without a readiness probe, traffic reaches pods that are not ready (5xx); without `preStop` and graceful shutdown, active connections are cut. A PodDisruptionBudget protects the minimum available pod count while draining a node. In production, let a GitOps controller such as ArgoCD apply changes instead of running kubectl manually.\n\n- When a pod dies for no obvious reason, trace `OOMKilled` and exit code 137 as described in (21-linux-production-debug.diagnosing-the-system.q4).\n:::",
          "translated": true,
          "difficulty": "core"
        },
        {
          "id": "14-devops-k8s-best-practices.docker-kubernetes.q5",
          "q": "What roles do Service, Ingress, ConfigMap, and Secret play?",
          "a": "- [[b:Service]]: provides a stable address and load-balances across a set of pods (ClusterIP, NodePort, LoadBalancer)\n- [[b:Ingress]]: routes external HTTP(S) traffic to services by host/path and terminates TLS through an ingress controller such as Nginx\n- [[b:ConfigMap]]: stores non-sensitive configuration\n- [[b:Secret]]: stores sensitive data, which should be encrypted at rest with KMS\n\nBoth ConfigMaps and Secrets can be injected through environment variables or volumes.\n\n:::deep\n- a Service targets pods through a label selector; its forms include ClusterIP (internal), NodePort, LoadBalancer (cloud LB), and Headless (per-pod DNS for a StatefulSet)\n- Ingress provides L7 routing and TLS termination through Nginx/Traefik; Gateway API is its modern successor\n- [[r:A Secret is only base64-encoded; it is NOT encrypted by default]] → enable encryption at rest for etcd, enforce RBAC, and keep secrets out of logs\n- a Service decouples the stable 'address' from ephemeral pods: pod IPs change, while Service DNS remains stable\n\n[[r:Pitfall]]: treating a Secret as inherently secure. Production systems need KMS, Sealed Secrets, or External Secrets backed by Vault; ConfigMap changes do not reload applications automatically, so use a restart or reloader. For an OTA platform, source PSP/GDS API keys from Vault through External Secrets.\n:::",
          "translated": true,
          "difficulty": "core"
        },
        {
          "id": "14-devops-k8s-best-practices.docker-kubernetes.q6",
          "q": "Why are liveness, readiness, and startup probes separate?",
          "a": "<table class=\"cmp\"><thead><tr><th>Probe</th><th>Question</th><th>On failure →</th></tr></thead><tbody>\n<tr><td>Liveness</td><td>'is it alive?'</td><td>restart the container</td></tr>\n<tr><td>Readiness</td><td>'can it accept traffic?'</td><td>remove it from the Service (no restart)</td></tr>\n<tr><td>Startup</td><td>'has startup completed?'</td><td>defer liveness during warmup</td></tr>\n</tbody></table>\n\n:::warn Pitfall\nCombining liveness with an external dependency such as the database means a slow database can trigger repeated pod restarts and a crash loop. Liveness should only establish that the process is healthy; readiness should reflect dependency availability.\n:::\n\n:::deep\n- [[b:Liveness]] asks 'is the process healthy?' → *restart* it on failure; perform only a lightweight internal check and [[r:DO NOT check the database]], because a slow database would create a useless restart loop\n- [[b:Readiness]] asks 'is it ready?' → *remove it from the Service* without restarting; check dependencies here, so a slow database removes the pod from the load balancer and lets it recover instead of killing it\n- [[b:Startup]] asks 'has startup completed?' for a slow-starting application such as a warming JVM → defer liveness/readiness so Kubernetes does not kill it during boot\n\n[[r:Classic failure]]: a liveness probe checks the database → the database fails → every pod restarts together → cascading failure plus lost warmup. In Spring Boot, use a startup probe and separate Actuator health groups for liveness and readiness.\n\n- A restart loop is not always probe-related: `OOMKilled` with exit code 137 is the other branch. Inspect `kubectl describe` before changing probes (21-linux-production-debug.diagnosing-the-system.q4).\n:::",
          "translated": true,
          "difficulty": "hard"
        }
      ]
    },
    {
      "title": "Web server, networking & observability",
      "items": [
        {
          "id": "14-devops-k8s-best-practices.web-server-networking-observability.q1",
          "q": "How does Nginx work as a reverse proxy and load balancer?",
          "a": "Nginx uses an event-driven, non-blocking architecture and sits in front of the application:\n- [[b:reverse proxy]]: hides backends, terminates TLS, manages headers, and applies gzip compression\n- [[b:load balancing]]: supports round-robin, least_conn, and ip_hash across an `upstream`\n- caches static content, rate-limits requests with `limit_req`, and buffers traffic\n\nIt is well suited to the edge/ingress role and serves static content or proxies traffic very efficiently with few resources.\n\n:::deep\n- important settings include `proxy_buffering off` for SSE/streaming; upstream `keepalive` to reuse connections and reduce handshakes; `proxy_read_timeout` for slow backends such as GDS; and `X-Forwarded-For`/`proto` so the backend knows the original client and protocol\n- Nginx can serve HTTP/2 on the client-facing side while using HTTP/1.1 to upstreams\n\nIn an OTA platform, Nginx/Envoy at the edge handles TLS, coarse-grained rate limiting, and routing; an application gateway such as Spring Cloud Gateway handles authentication and business-aware routing. [[g:Envoy]] underpins service meshes: it understands HTTP/2/gRPC and provides configuration-driven retries and circuit breakers, making it an increasingly common replacement for Nginx inside the mesh.\n:::",
          "translated": true,
          "difficulty": "core"
        },
        {
          "id": "14-devops-k8s-best-practices.web-server-networking-observability.q2",
          "q": "How do you manage high-volume logs with structured logging and ELK/Loki?",
          "a": "At multi-service scale, [[r:do not SSH into hosts to read log files]]. Instead:\n- emit [[g:structured logs]] as JSON with standard fields such as service, level, trace_id, request_id, and an anonymized user_id\n- collect logs centrally in [[b:ELK]] (Elasticsearch + Logstash/Fluentd + Kibana) or [[b:Loki]] (lower cost, label-based indexing, and a natural fit with Grafana)\n- define retention/rotation policies and sample noisy logs\n- [[r:DO NOT log sensitive data]] such as PII, PCI data, or tokens\n\n:::tip Key point\nAttach trace_id/request_id to every log so you can reconstruct one request's path across services—the foundation of distributed debugging.\n:::\n\n:::deep\n- emit structured JSON with required fields (timestamp, level, service, trace_id, span_id, request_id); ship it through an agent such as Fluent Bit/Vector → buffer it in Kafka → ingest it into ELK/Loki\n- control cost by sampling noisy logs, separating business logs from debug logs, and tiering retention (for example, 7 days hot and a cold archive in S3)\n- [[r:Never log PII/PCI data]] such as card numbers, tokens, or passwords; this is a common compliance violation, so apply masking\n\nFor an OTA platform, log every GDS/PSP interaction with a correlation ID so teams can reconcile records and investigate stuck transactions.\n:::",
          "translated": true,
          "difficulty": "hard"
        },
        {
          "id": "14-devops-k8s-best-practices.web-server-networking-observability.q3",
          "q": "How does distributed tracing work in Kubernetes with OpenTelemetry and Jaeger?",
          "a": "- every request carries a [[b:trace ID]]; each hop is a [[b:span]] with a parent, except for the root span; context is propagated through HTTP/gRPC headers and message headers\n- [[g:OpenTelemetry]] provides vendor-neutral instrumentation and exports data to [[b:Jaeger]], Tempo, or Datadog\n- a trace shows which services handled a request and where time was spent—for example, 'checkout is slow because a synchronous fraud-check call adds 600 ms'\n- in Kubernetes, run the OTel Collector as a DaemonSet or sidecar\n\n:::deep\n- OTel standardizes creation &amp; propagation of *trace context* using the W3C `traceparent` header → each hop creates a span that is a child of its parent and shares the same trace_id; use auto-instrumentation for HTTP/gRPC/JDBC/Kafka plus manual spans, then export OTLP → collector → backend\n- one request becomes a *span tree* that exposes the duration of each hop; [[b:sampling]] at the head or tail controls cost, with tail sampling retaining failed or slow traces\n- the real value is [[g:correlation]]: add trace_id to logs and metric exemplars, and propagate it in Kafka headers to trace asynchronous flows\n\nIn an OTA platform, a 'book ticket' trace across booking→payment→gds→ticketing can reveal that GDS contributes 600 ms of latency; [[r:without traces, finding that bottleneck is like looking for a needle in a haystack]].\n:::",
          "translated": true,
          "difficulty": "hard"
        },
        {
          "id": "14-devops-k8s-best-practices.web-server-networking-observability.q4",
          "q": "What are the three pillars of observability and the RED/USE metrics?",
          "a": "The three pillars are:\n- [[b:Logs]] (structured and correlated with a trace ID)\n- [[b:Metrics]] (Prometheus + Grafana)\n- [[b:Traces]] (OpenTelemetry→Jaeger)\n\nMeasurement frameworks:\n- [[g:RED]] for services: Rate, Errors, Duration\n- [[g:USE]] for resources: Utilization, Saturation, Errors\n\nAlert against SLOs/SLIs rather than alerting on every spike.\n\n:::deep\n- a metric says 'something is wrong' → a trace locates 'where' → logs explain 'why'\n- [[o:alert on SLOs and user-visible symptoms]] such as p99 latency or user-visible error rate, not on causes such as CPU reaching 80%, which may not represent a problem → this reduces alert fatigue\n- saturation, such as pending work in a queue or pool, is a leading indicator; business metrics such as `booking_failed_total` and `payment_pending` are as important as infrastructure metrics\n\nFor an OTA platform, combine a RED dashboard for each service with the business funnel (search→hold→pay→issue) to identify the exact step where the workflow breaks.\n\n- Topic 20 covers the full treatment: which metrics to choose for services and resources (20-observability-sre.the-three-pillars-how-to-measure.q3), why one bad label can take down the entire metrics system (20-observability-sre.the-three-pillars-how-to-measure.q4), what SLOs and error budgets help decide (20-observability-sre.slos-alerting-incidents.q1), and how to make logs searchable enough for investigations (20-observability-sre.slos-alerting-incidents.q4).\n:::",
          "translated": true,
          "difficulty": "core"
        }
      ]
    },
    {
      "title": "CI/CD & 2026 best practices",
      "items": [
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q1",
          "q": "What does a typical CI/CD pipeline look like, and how do blue-green and canary deployments compare?",
          "a": "Stages: source → build → unit test → SAST/SCA scan → immutable, versioned image → deploy to staging → integration test → gated production deployment → smoke test → rollback readiness.\n\n<table class=\"cmp\"><thead><tr><th></th><th>Blue-green</th><th>Canary</th></tr></thead><tbody>\n<tr><td>Method</td><td>maintain two environments and switch all traffic</td><td>shift a growing percentage of traffic gradually</td></tr>\n<tr><td>Rollback</td><td>immediate (switch back)</td><td>stop or reduce the percentage</td></tr>\n<tr><td>Cost</td><td>roughly double the resources</td><td>requires strong observability</td></tr>\n<tr><td>Blast radius</td><td>all users if smoke tests miss the defect</td><td>small</td></tr>\n</tbody></table>\n\n:::tip Fintech\nPrefer canary releases, feature flags, and backward-compatible database migrations so rollback remains safe.\n:::\n\n:::deep\n- pipeline: commit → build + unit test → SAST + SCA (CVE) + secret scan → build an image pinned by digest + sign it with cosign → push to the registry → staging + integration/contract tests → approval → production\n- [[r:Database migration is the hardest part]]—use *expand-contract*: add a backward-compatible column first, deploy the code, then remove the old column later, so rollback remains possible\n\nFor OTA/fintech systems, use canaries for high-risk payment-flow changes, and use feature flags to separate 'deployment' from 'release' (Argo Rollouts plus a mesh for traffic splitting).\n:::",
          "translated": true,
          "difficulty": "core"
        },
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q2",
          "q": "What problems does a service mesh such as Istio or Linkerd solve, and what are the 2026 best practices?",
          "a": "A service mesh moves networking cross-cutting concerns—retries, circuit breakers, [[g:mTLS]], traffic splitting/canaries, and observability—into the infrastructure layer through a [[b:sidecar proxy]] instead of application code.\n\n- in 2026, Istio and Linkerd are production-ready: Linkerd is lightweight and tends to 'just work', while Istio offers a broader feature set\n- start with one capability, such as mTLS or canary delivery, and monitor sidecar and control-plane resource usage\n\n:::deep\n- extract service communication logic (mTLS, retries, timeouts, circuit breakers, traffic splitting, observability) into an Envoy sidecar → applications do not implement network resilience/security themselves; declarative policies configure it\n- [[g:benefits]]: automatic mTLS and certificate rotation for zero trust, traffic management for canary/A-B releases, and uniform telemetry with automatic RED metrics\n- [[r:costs]]: added sidecar latency and resources plus operational complexity; the 2026 direction is Istio *ambient mesh*, which removes the per-pod sidecar\n\n[[r:Do not introduce a mesh too early]]: for a handful of services, resilience4j plus the OTel SDK is enough. A mesh becomes valuable when many polyglot services need consistent mTLS and traffic policies.\n:::",
          "translated": true,
          "difficulty": "ext"
        },
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q3",
          "q": "What are the leading backend architecture practices for 2025–2026?",
          "a": "- [[g:Database per service]]: services DO NOT share a database, which prevents a distributed monolith\n- [[g:API Gateway]]: centralizes OAuth/JWT validation, protocol translation (REST→gRPC), and aggregation\n- [[g:OpenTelemetry]]: standardizes vendor-neutral tracing; pair ELK/Loki with Prometheus/Grafana\n- [[g:AIOps]]: anomaly detection, predictive alerting, and automated remediation\n- [[g:Platform engineering / IDP]]: standardizes CI/CD, observability, and templates so a developer can provision a service with one command\n- [[g:Serverless containers]]: suit variable workloads; use GraalVM native images/ZGC for scale-to-zero workloads\n- [[g:Polyglot architecture]]: use Java for the business core and Go at the edge or for high-throughput components, interoperating through gRPC\n\n:::tip Key point\nThe common direction is to push complexity into the infrastructure/platform layer—mesh, gateway, IDP, and AIOps—so services can focus on business logic.\n:::\n\n:::deep\n- *platform engineering* (an IDP with golden paths and self-service) replaces the model where 'DevOps does everything'; combine *database per service* with event-driven integration (Kafka/Outbox/CDC); use an *API gateway* for authentication/rate limiting and REST↔gRPC; use *OTel* to standardize observability; apply *AIOps* for ML-assisted RCA; combine *GitOps* (ArgoCD/Flux) with IaC (Terraform); adopt a *mesh*, zero trust, and mTLS; consider serverless containers; secure the *software supply chain* with SBOMs, signed images, and SLSA\n- the recurring themes are [[g:lower coupling + automation + observability + secure by default]]; the recurring warning is that [[r:a distributed monolith is the number-one anti-pattern]]\n- a senior engineer in 2026 considers operational and cloud cost through FinOps, not merely whether a technology is fashionable\n:::",
          "translated": true,
          "difficulty": "ext"
        },
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q4",
          "q": "Why are GitOps and Infrastructure as Code important?",
          "a": "- [[b:IaC]] (Terraform): declares infrastructure as versioned, reproducible code instead of configuring it manually through a UI\n- [[b:GitOps]] (ArgoCD/Flux): makes Git the source of truth for cluster state; an agent reconciles the cluster to Git → auditable changes, rollback through git revert, and no drift\n\nThis model suits compliance-heavy environments such as fintech because every change leaves a trace.\n\n:::deep\n- IaC (Terraform/Pulumi) makes infrastructure reproducible, versioned, and reviewable while avoiding 'snowflake servers'; plan/apply exposes changes before execution\n- GitOps makes Git the *single source of truth*; a controller reconciles the cluster to Git, every change goes through a PR (audit + review + rollback = git revert), and drift is detected\n- the result is [[g:declarative + auditable + self-healing]] delivery: deployment means merging a PR; keep secrets out of Git with Sealed Secrets or External Secrets\n\nFor fintech/OTA systems, an audit trail for every infrastructure and deployment change is a compliance requirement. [[r:Pitfall]]: Terraform state can contain sensitive data, so store it securely and lock it.\n:::",
          "translated": true,
          "difficulty": "ext"
        },
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q5",
          "q": "What does zero-trust security mean in microservices?",
          "a": "Reject the assumption that the internal network is trusted. Every request—including service-to-service traffic—must be authenticated and authorized with mTLS plus a token, granted least privilege, encrypted in transit, and constrained by network segmentation through network policies.\n\n- monolith: protect the perimeter\n- microservices: [[g:'there is no inside']] → every service verifies every request itself\n\nA service mesh can implement zero-trust mTLS without application-code changes.\n\n:::deep\n- [[g:'never trust, always verify']]—every request, including service↔service traffic inside the cluster, must be authenticated, authorized, and encrypted\n- implementation: mTLS between every service, with the mesh issuing SPIFFE identities and rotating them automatically; least privilege through NetworkPolicy, minimal RBAC, and a dedicated service account; tokens/JWTs that carry user context across hops\n- rationale: if one pod is compromised, lateral movement becomes possible → zero trust limits the [[g:blast radius]], unlike a 'castle-and-moat' model where breaching the perimeter exposes everything\n\nFor OTA/fintech systems handling PII and PCI-DSS-scoped payment data, zero trust, encryption, and auditing are mandatory; the minimum set is NetworkPolicy, mesh mTLS, secret management, RBAC, and step-up authentication.\n:::",
          "translated": true,
          "difficulty": "ext"
        },
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q6",
          "q": "How do graceful shutdown and connection draining keep Kubernetes rolling updates from dropping in-flight requests?",
          "a": "When replacing a pod, Kubernetes begins the termination grace period, runs any configured lifecycle hook, sends [[b:SIGTERM]], and sends [[b:SIGKILL]] only after `terminationGracePeriodSeconds` expires.\n\nThe application must:\n- stop accepting new requests\n- finish [[o:in-flight requests]]\n- close DB/MQ pools before exiting\n\nFor Spring Boot, configure `server.shutdown=graceful` and `spring.lifecycle.timeout-per-shutdown-phase`.\n\n[[g:Critical detail]]: make the pod unready as soon as termination begins so it is removed from Service endpoints, and add a `preStop` hook that sleeps for a few seconds so the load balancer stops routing before the process exits.\n\n:::deep\n[[r:Classic race condition]]: SIGTERM reaches the application *before* kube-proxy/Ingress has updated the endpoint &#8594; traffic continues reaching the terminating pod &#8594; clients receive 502 responses or connection resets.\n\nThe robust pattern is to make readiness=false as termination begins and use a 5–15 second `preStop` sleep while endpoint removal propagates; only then shut down the server. Set `terminationGracePeriodSeconds` &gt; the longest request duration, such as a 30-second PSP call. A Kafka/Rabbit consumer should stop polling, commit remaining offsets or acknowledgments, and leave the group cleanly.\n\nFor fintech/OTA systems, [[r:never allow SIGKILL between 'money debited' and 'ticket issued']]; drain work and use a Saga/outbox so an incomplete step continues after restart. Graceful shutdown is required for rolling updates, autoscaling, or spot-instance termination to avoid losing transactions.\n:::",
          "translated": true,
          "difficulty": "hard"
        },
        {
          "id": "14-devops-k8s-best-practices.ci-cd-2026-best-practices.q7",
          "q": "How do versioned migrations with Flyway/Liquibase and expand-contract change a live database with zero downtime?",
          "a": "Use [[g:versioned migrations]] with Flyway/Liquibase: each change is an immutable, checksummed file that runs automatically &amp; in the correct order during deployment.\n\nApply [[g:expand-contract]] as a parallel change across several releases for zero downtime:\n- [[b:expand]]: add a new nullable column/table, backfill it, and make the code write both old and new structures\n- [[b:migrate]]: switch reads to the new structure\n- [[b:contract]]: remove the old structure in a later release\n\nEvery step remains backward-compatible so old and new pods can run together during a rolling update.\n\n:::deep\n[[r:Why not 'ALTER, then deploy']]: a rolling update temporarily runs *both* old and new pods &#8594; the schema must support both versions; abruptly renaming/dropping a column or adding `NOT NULL` breaks old pods. The safe way to add NOT NULL is: add nullable &#8594; backfill in batches &#8594; set NOT NULL afterward.\n\n[[r:Large-table pitfall]]: a MySQL ALTER can lock the table, make DDL expensive &amp; cause replication lag → use online DDL such as `ALGORITHM=INPLACE` or gh-ost/pt-online-schema-change, and backfill in small batches. Run the backward-compatible migration *before* deploying code that reads the new structure.\n\nIn fintech, [[r:never drop a monetary/ledger column until you are certain no readers remain]]; every migration needs a rollback plan. Expand-contract discipline enables several deployments per day without a maintenance window.\n:::",
          "translated": true,
          "difficulty": "hard"
        }
      ]
    }
  ]
}
